# 3DGameStudio World Runtime 安定化・物理描画改修

対象リポジトリ:
https://github.com/smorce/3DGameStudio

ベースブランチ:
feat/procedural-world-runtime

このブランチを直接変更せず、新しいブランチを作成してください。

新ブランチ名:
fix/runtime-stability-rebase-shadow-colliders

目的:
現在確認されている以下の不具合を、個別の場当たり修正ではなく、
World Runtime / Physics / Renderer の責務を整理しながらまとめて修正してください。

作業は調査だけで終わらせず、実装、テスト、実動作確認、証拠保存、
文書更新まで完了してください。


# 0. 最重要方針

今回の改修では「症状を隠す修正」を禁止します。

例えば、

- HingeのSolver Iterationを極端に増やして隙間を隠す
- Shadow Cameraを巨大化して影切れを隠す
- CCDを全面的に無効化して空中停止を回避する
- Colliderを適当に巨大化してめり込みを隠す
- カメラの平滑化だけで機体のカクつきを隠す

といった対応はしないでください。

まず再現し、原因を計測し、設計上の原因を直してください。

既存のCar / Plane / Boat / Editor / Course / Save / Load / Asset / LOD /
Physics Streamingを壊さないことも必須です。


# 1. 最初に現在状態を保存する

作業開始時に以下を実施してください。

1. feat/procedural-world-runtime を最新化
2. git status がcleanであることを確認
3. fix/runtime-stability-rebase-shadow-colliders を作成
4. 現在のテスト結果を保存
5. 現在のStarter Planeで問題を再現
6. 修正前のTelemetry、rebaseCount、位置、速度を記録

最低でも以下を実行してください。

pnpm typecheck
pnpm lint
pnpm test
pnpm build
pnpm smoke
pnpm telemetry:plane

結果は

docs/evidence/runtime-stability-before.json

または同等の証拠ファイルへ保存してください。


# 2. 今回修正する不具合

以下をすべて対象にしてください。


## 問題A: World Entityの見た目とColliderが一致していない

現在のprocedural entityでは、tree / building / rock等のColliderが
基本的にscaleから作った同じようなcuboidになっています。

一方、Rendererでは、

tree:
- 幹: Cylinder
- 葉: Cone

building:
- 本体: 2 x 2.6 x 2
- 屋根あり

rock:
- Icosahedron

という異なる形状を描画しています。

このため建物や木に機体が大きくめり込んでから衝突します。


### 修正方針

RendererとPhysicsで寸法を別々に定義しないでください。

builtin entity用の共有定義を新設してください。

例:

BuiltinEntityDefinition
- visual dimensions
- collider type
- collider size
- collider local position
- collider local rotation

RendererとPhysicsの両方が、この共有定義を参照する構造にしてください。

依存方向を壊すため、
Physicsからrenderer-threeをimportする設計は禁止します。

world-system / world-generator / project-schemaなど、
RendererとPhysics双方から参照可能な場所へ定義してください。


### Colliderの目安

tree:
- 少なくとも幹をCylinder Colliderとして扱う
- 幹 半径 約0.2m
- 高さ 約1.5m
- 中心Y 約0.75m
- 葉全体を巨大なBox Colliderにしない

building:
- 本体を少なくとも2 x 2.6 x 2のBoxとして扱う
- 中心Y 約1.3m
- 必要なら屋根用Colliderを分離する

rock:
- Ball / Convex /適切な近似形状を使う

entity.transformの
position / rotation / scale
を正しく反映してください。

現在のようにscaleだけを使いrotationを無視しないでください。


### テスト

tree / building / rockについて、

- 描画定義とCollider定義のサイズ
- scale適用
- rotation適用
- translation適用

をUnit Testしてください。

将来Visualだけ変更してColliderを更新し忘れる構造に戻さないでください。


# 3. 問題B: 飛行中に機体が空中で停止する

確認されている症状:

- Starter Planeで飛行可能
- 飛行中に突然その場所から動かなくなる
- HUD上では速度が残っている
- おおむね長距離移動後に発生する


# 4. 問題C: Rebase後にエルロン・昇降舵・方向舵だけ隙間が開く

重要な観測結果:

固定Panel同士ではなく、

- Aileron
- Elevator
- Rudder

などRevolute Jointで接続された可動翼だけに隙間が発生する。

したがってOrigin Rebaseと複数RigidBody / Jointの相互作用を
最優先で調査してください。


# 5. Origin Rebasingを設計し直す

現在はRapierPhysics.step()内部で、

world.step()
↓
applyOriginRebase()
↓
WorldRuntime.maybeRebase()
↓
全RigidBody.setTranslation()

を行っています。

その後EngineがlastRebaseDeltaを受け取り、
Renderer.applyOriginShift()を呼んでいます。

この責務分散を廃止してください。


## 必須アーキテクチャ

Origin RebaseはEngine側から
「1回のTransaction」として実行してください。

概念:

Engine Fixed Update
    |
    +-- physics.step()
    |
    +-- WorldRuntime.planRebase(globalFocus)
            |
            +-- deltaなし → 継続
            |
            +-- deltaあり
                    |
                    +-- Physics.shiftOrigin(delta)
                    +-- Renderer.shiftOrigin(delta)
                    +-- WorldRuntime.commitRebase(delta)

重要:

RapierPhysics.step()自身がWorldRuntime.maybeRebase()を呼ばないこと。

Physics自身が世界座標を勝手に変更しないこと。

WorldRuntimeは

- global coordinate
- simulation coordinate
- worldOrigin
- rebase計画

を管理し、

Engineが

- Physics
- Renderer
- WorldRuntime

を同期させてください。


## WorldRuntime API

現在のmaybeRebase()はworldOriginをその場で変更します。

これを、

planRebase(focusGlobal)

と

commitRebase(plan)

のように、

「判定」と「commit」を分離することを推奨します。

名前は変更して構いません。

重要なのは、
Consumerが半分だけ新しい座標系になった状態を作らないことです。


# 6. Physics.shiftOrigin(delta)

新しい明示APIを作ってください。

例:

physics.shiftOrigin(delta)

この処理はPhysics StepとPhysics Stepの間だけで実行します。


## 必須条件

同じdeltaを、

- chassis
- fixed machine bodies
- Aileron body
- Elevator body
- Rudder body
- その他のDynamic RigidBody
- static terrain collider
- generated entity collider
- course collider

へ整合して反映してください。

Dynamic RigidBodyだけ動かしてStatic Colliderを後で更新する、
という中間状態を作らないでください。


## Rapierの位置同期

RigidBodyを手動移動した場合は、
Rapierの

world.propagateModifiedBodyPositionsToColliders()

の使用を検討し、
現在使用しているRapier 0.19系で実際に動作確認してください。

RigidBodyに付属するColliderとStandalone Colliderの違いも考慮してください。


## 必ず維持するもの

Rebase前後で、

linear velocity
angular velocity
rotation
Joint relative angle
control input
motor target
machine global position

を変化させないでください。

Rebaseは座標系の変更であり、
物理イベントとして機体へ力を加えてはいけません。


## CCD

現在Dynamic BodyでCCDを使っています。

CCDを一律無効化して問題を回避するのは禁止します。

RebaseがCCDに影響している場合は、
Rebase前後の状態を調査し、
正しい整合方法を実装してください。

必要であればRebase専用処理を入れて構いませんが、
通常飛行中のCCDは維持してください。


# 7. Hingeの健全性を計測する

Revolute Jointについて、
デバッグまたはテスト用にJoint Anchor Errorを計測できるようにしてください。

最低対象:

- left aileron
- right aileron
- left elevator
- right elevator
- rudder

Rebase直前・直後でAnchor間距離を記録してください。

通常のStarter Planeでは、
可動翼が目視できるほど離れる状態を許可しません。

目標:
anchor separation <= 0.02～0.03m程度

最終的な許容値は実測を基に決めて構いませんが、
「数十cm離れていてもテストPASS」は禁止です。


## Solver Iteration

正しいRebase処理を入れた後でも、
通常飛行中にHinge誤差が大きい場合だけ、

setAdditionalSolverIterations()

等を検討してください。

最初からSolver Iterationを増やして症状を隠さないでください。


# 8. 問題D: 飛行機の表示がカクつく

Physicsは1/60秒固定更新を維持してください。

物理更新頻度をRendererのFPSに合わせて可変にしないでください。


## Render Interpolationを実装

前回Physics Pose
current Physics Pose

を保持し、

alpha = accumulator / fixedDt

を使って描画してください。

Position:
lerp(previous, current, alpha)

Rotation:
quaternion slerp(previous, current, alpha)

を使用してください。

対象は少なくともMachine Partsです。


## Rebaseとの統合

これは非常に重要です。

Rebaseをまたいだ瞬間、

previous pose = 旧simulation座標
current pose = 新simulation座標

のままInterpolationすると、
巨大なジャンプが発生します。

したがってRebase時には、

- previous/currentの両方を同じdeltaで変換する

または

- Rebaseフレームのみprevious=currentへリセットする

など、座標系を必ず一致させてください。


# 9. Camera Followを時間基準にする

現在のような、

lerp(..., 0.08)

という「1フレームにつき8%」方式をやめてください。

frame delta timeを使った追従にします。

例:

follow = 1 - exp(-lambda * dt)

Renderer FPSが30 / 60 / 120で変化しても、
カメラ追従時間がほぼ同じになるようにしてください。

Render InterpolationとCamera Followは分離して設計してください。


# 10. 問題E: 影が突然現れたり消えたりする

現在DirectionalLight Shadow Cameraが
原点周辺の固定範囲になっています。

Player / Machineの移動に合わせてShadow領域を追従させてください。


## 必須方針

DirectionalLightの

- light.position
- light.target.position

をPlayerまたはCamera Focusに追従させます。

光の方向自体はtimeOfDayから決まる現在の方向性を維持してください。

sun.targetをsceneへ正しく追加してください。

Shadow CameraはPlayer周辺だけをカバーしてください。

巨大なShadow Frustumにして誤魔化さないでください。

目安として40～60m程度から評価し、
現在のWorldの視距離に合わせて調整してください。

Shadow mapSizeは必要以上に増加させないでください。

Shadow Cameraのleft/right/top/bottom/near/farを変更した場合は
Projection Matrixも正しく更新してください。


## Rebase時

Sun
Sun Target
Camera
Camera Target

も同じ座標系に存在するようにしてください。

Rebase境界を通過した瞬間だけShadowが消える状態を禁止します。


# 11. Procedural Chunk生成によるカクつきも確認する

WorldRuntime.getChunk()では、
cache miss時にgenerateChunk()を同期実行しています。

一方、world-generatorにはWorker向けコードがあります。

まず実測してください。


## 計測

Play中にChunk生成が、

- requestAnimationFrameを長時間止めていないか
- frame time spikeの原因になっていないか

を測定してください。

generationLatencyMsだけでなく、
Play Frame時間との相関も確認してください。


## Worker化の条件

同期Chunk生成が実際に飛行中の大きなカクつきへ寄与している場合は、
Play中の先読み生成をWeb Workerへ移してください。

ただしPhysicsが必要な地形が未生成になる改悪は禁止します。

推奨:

velocity方向へ現在より先のChunkを非同期prefetch
↓
共有WorldRuntime Cacheへcommit
↓
Physics/Rendererが必要になる前に準備

通常飛行の長距離テストで、
同期fallback generationが発生しない状態を目標にしてください。

Worker化が不要と判断した場合は、
実測値と理由を最終報告に残してください。


# 12. Rebase Regression Testを新設する

これが今回もっとも重要です。

現在の短距離テストだけでは不十分です。


## Starter Plane Long Distance Test

Starter Planeを最低500m、
できれば1000m程度飛ばしてください。

現在のRebase閾値を考慮し、
最低でも複数回のRebaseを発生させます。

検査:

rebaseCount >= 3
global positionが連続
NaN / Infinityなし
速度が有限
rotationが有限
linear velocityをRebase自身が変更しない
angular velocityをRebase自身が変更しない
control surfacesが分離しない
terrain streamingが継続
render streamingが継続


## 空中停止Regression

直進飛行テストでは、

worldSpeed > 5m/s
かつ
throttle > 0

の状態が一定時間続いているのに、
global positionがほぼ変化しない状態を失敗としてください。

「HUDの速度だけ残り位置が止まる」状態を自動検出してください。


## Global Position continuity

Rebaseの瞬間に、

globalPosition

がdelta分ジャンプしてはいけません。

simulationPositionがRebaseで変化するのは正常です。

Telemetryで、

globalPosition
simulationPosition
worldOrigin
rebaseCount

を同時に検証してください。


## Joint Test

Rebase前後の各Control Surfaceについて、

joint anchor separation
relative angle
angular velocity

を比較してください。


# 13. Shadow Regression Test

可能な範囲で自動テストしてください。

最低でも、

- Playerが原点付近
- Rebase直前
- Rebase直後
- 200m以上移動後

でDirectionalLightとtargetがPlayer周辺に存在することを検査してください。

Playwrightで決定論的なScreenshotを保存できる場合は、

docs/evidence/runtime-stability-shadow/

等へ保存してください。


# 14. Collider Regression Test

Tree / Buildingへ低速で機体またはテストBodyを接近させ、

Visualから大きくめり込んでからContactする状態をなくしてください。

特にBuildingは、
建物本体のおおよその外壁位置でContactすることを検査してください。


# 15. 既存機能のRegression

以下を必ず維持してください。

Car
Plane
Boat
finite world
procedural world
terrain streaming
world entity streaming
course
external asset collider
LOD
instancing
save/load
schema migration
editor
play/stop
respawn

今回と無関係なschema変更は避けてください。


# 16. デバッグ機能

今回の問題を今後調査しやすくするため、

Development時に確認可能な以下の情報を追加してください。

- rebaseCount
- worldOrigin
- simulationPosition
- globalPosition
- current chunk
- world speed
- Joint Anchor Errorの最大値
- procedural generation latency
- synchronous generation fallback count

既存Runtime Statsへ自然に追加できるものは追加してください。

本番画面を過度に汚さないでください。

Collider Debug表示も、
RapierのdebugRender()等を使って簡単に切り替えられるなら追加して構いません。


# 17. 修正対象として最初に確認するファイル

最低限以下を読んでから実装してください。

packages/physics-rapier/src/index.ts
packages/engine-core/src/index.ts
packages/renderer-three/src/index.ts
packages/renderer-three/src/instances.ts
packages/machine-system/src/index.ts
packages/world-system/src/runtime.ts
packages/world-system/src/streaming.ts
packages/world-system/src/coordinates.ts
packages/world-generator/src/index.ts
packages/world-generator/src/worker.ts
packages/runtime-telemetry/src/index.ts

tests/unit/world-runtime.test.ts
tests/unit/renderer-transforms.test.ts
tests/integration/physics.test.ts
tests/integration/world-streaming.test.ts
tests/e2e/world-runtime.spec.ts

docs/procedural-world-architecture.md


# 18. アーキテクチャ上の禁止事項

禁止:

PhysicsがRendererへ依存
RendererがRapierへ直接依存
Physics.step()内部でworldOriginを勝手に変更
Rendererが独自のworldOriginを持つ
VisualとColliderの寸法を別々のmagic numberで管理
RebaseごとにMachineを作り直す
RebaseごとにJointを作り直す
CCD全面無効化
既存テスト削除
テスト期待値を緩めてPASSさせる
問題をKNOWN_ISSUESへ書くだけで終了
Starter Plane専用のif文で問題を回避

Car / Plane / Boat共通で正しく動くWorld Runtimeとして実装してください。


# 19. 推奨する責務

WorldRuntime
- global/simulation変換
- worldOrigin
- rebase plan / commit
- chunk cache
- generation/prefetch

Engine
- fixed timestep orchestration
- rebase transaction
- interpolation alpha
- consumer synchronization

RapierPhysics
- physics simulation
- rigid body / collider / joint
- shiftOrigin(delta)
- physics pose snapshots
- physics telemetry

ThreeRenderer
- render interpolation
- camera follow
- shadow follow
- render-side origin shift

Machine System
- machine topology
- fixed-body grouping
- joint/control surface definitions

Builtin World Entity Definition
- visual/collider双方が使う共通寸法


# 20. 完成条件

以下をすべて満たして初めて完成としてください。

Starter Planeが500m以上飛行できる
複数回Rebaseしても飛行を継続する
速度だけ残って空中停止しない
Aileronが離れない
Elevatorが離れない
Rudderが離れない
Global PositionがRebaseで飛ばない
Shadowが移動中に突然消えない
ShadowがRebase境界で突然切り替わらない
飛行機の表示がRender Interpolationされる
Camera FollowがFPS依存ではない
Tree Colliderが見た目から大きくズレない
Building Colliderが見た目から大きくズレない
既存Carが動く
既存Boatが動く
既存Planeが動く

さらに、

pnpm typecheck
pnpm lint
pnpm format:check
pnpm test
pnpm test:integration
pnpm test:e2e
pnpm smoke
pnpm build

をすべてPASSさせてください。


# 21. Evidenceを残す

以下を新規作成してください。

docs/evidence/runtime-stability-before.json
docs/evidence/runtime-stability-after.json
docs/runtime-stability-report.md

必要なら追加で、

docs/evidence/runtime-stability-flight.jsonl
docs/evidence/runtime-stability-rebase.json
docs/evidence/runtime-stability-shadow/

を作って構いません。


afterには最低限、

- flight distance
- maximum speed
- rebase count
- maximum global-position discontinuity
- maximum hinge anchor error
- synchronous generation fallback count
- generation latency
- tests result

を入れてください。


# 22. 文書更新

実装後、

docs/procedural-world-architecture.md

を実装と一致させてください。

KNOWN_ISSUES.mdについても、
直った問題を「Known Issue」のまま残さないでください。

一方、未解決の制約は隠さないでください。


# 23. 最後のセルフレビュー

完了宣言前にdiff全体を読み直してください。

特に、

- Origin Rebase中に座標系が半分だけ切り替わる瞬間がないか
- Dynamic BodyとStatic Colliderが同じdeltaで移動しているか
- Joint Bodyが漏れていないか
- Rendererで二重にdeltaを引いていないか
- interpolationのprevious/currentが別座標系になっていないか
- Camera / Sun / Sun Targetが漏れていないか
- TelemetryがSimulation座標をGlobal座標として返していないか
- negative chunk座標を壊していないか
- finite worldを壊していないか

を確認してください。


# 24. 最終報告形式

作業完了後、以下の順で報告してください。

1. 新ブランチ名
2. 原因分析
3. アーキテクチャ変更内容
4. 変更ファイル一覧
5. Origin Rebaseの新しい流れ
6. Hinge分離問題の原因と修正
7. 空中停止問題の原因と修正
8. Render Jitterの修正
9. Shadowの修正
10. Colliderの修正
11. Chunk生成の調査結果
12. Before / After数値比較
13. 実行したテストと結果
14. 残っているKnown Issues
15. Commit SHA

「おそらく直った」ではなく、
自動テストとTelemetryの数値を根拠に完成判定してください。