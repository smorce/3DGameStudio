かなり改善しています。前回の主要なP0指摘だった **「Node上の疑似実行ではなく実ブラウザを走らせる」「実Workerを使う」「Worldの本当のLifecycleイベントを取る」「seedを実際のWorld生成へ適用する」「Before/After条件を厳密に比較する」** は修正されています。現在の先端は `fa30268` で、前回の `4d004fe` から1コミット進んでいます。

ただし、私はまだ **完全Approveではなく「軽めのRequest changes」** にします。残る重要項目は3つです。

| 優先度    | 項目                             | 判定        |
| ------ | ------------------------------ | --------- |
| P0     | ブラウザ実行・実Engine・実Worker・実Camera | ✅ 修正済み    |
| P0     | seed / 実World Lifecycle        | ✅ 修正済み    |
| P0     | Before/After同条件保証              | ✅ 修正済み    |
| **P0** | **失敗したRunの証拠保存**               | **❌ 要修正** |
| **P1** | **Scenario入力の厳密な再現性**          | **△ 要改善** |
| **P1** | **観測処理自身による性能汚染**              | **△ 要改善** |
| P1     | GPU timingのSummary接続           | △         |
| P1     | 11 ScenarioすべてのE2E保証           | △         |
| P2     | Player通常動作への影響                 | △         |

この3点を直せば、当初狙っていた**「AI自身がゲームを再現→観測→原因調査→修正→同条件再テスト」基盤としてApproveしてよい水準**だと思います。

## 【根拠】

まず、前回の最大の問題は解消しました。`pnpm observe run` は既定で `runBrowserObservationScenario()` を使い、`--mode=simulation` を明示した場合だけNode物理ランナーへ移ります。ブラウザ版はPlaywrightでStudioまたはPlayerを開き、実際にキー入力を行い、ScreenshotとPlaywright TraceまでRunへ保存します。

World Streamingも改善しています。ブラウザのEngineは `WorldRuntime` を `forceSyncWorkers: typeof Worker === "undefined"` で生成するため、通常のブラウザではWorker経路になります。また、WorldRuntime内部に `onLifecycleEvent` が入り、本当にQueueへ入った時に `world.chunk.queued`、生成完了時に `world.chunk.ready`、実際にCacheへCommitした時に `world.chunk.committed` を出しています。前回の「時間が0.01ミリ秒以上ならCommit扱い」といった推測ログはなくなっています。

seedも正しくなりました。ブラウザ版・Simulation版とも `createStarterWorld({ preset: scenario.worldPreset, seed: scenario.seed })` としており、Manifestへ記録するseedと実際の生成条件が一致しています。またScenario全体を `hashScenarioDefinition(scenario)` へ渡すようになったため、前回抜けていたWorld presetやtimelineなども変更検出対象になります。

Before/After比較も良くなりました。現在はScenario名だけでなく、Scenario定義のHash、seed、browser/node、Studio/Player、viewport、devicePixelRatioまで比較し、一致しなければ通常は比較を拒否します。これは今回の目的にはかなり重要です。

CameraとTurnも代理値から実測へ変わりました。Engineが実Rendererから `getMotionProbe()` を取得し、実Camera position、target、screen positionなどを記録しています。Run終了時には実際の `TurnMotionDiagnostics` と `CameraFollowToggleLog` の結果もObservationHubへ流しています。

---

### 残るP0：失敗したRunほど証拠が消えます

ここが一番直したいところです。

正常終了またはAssertion failureの場合はScreenshot、Trace、Events、Summaryが `.observability/runs/<runId>` へ保存されます。ところがブラウザ実行自体が例外になると、`catch` ではTraceを停止した後、その一時Traceを削除して、そのまま例外を投げ直しています。**error Runとして `writeObservationRun()` していません。**

つまり、

```text
ゲームがクラッシュ
↓
Playwright page error
↓
Scenario実行失敗
↓
一番調査したいRun
↓
保存されない
```

となり得ます。

今回の仕組みの目的を考えると、むしろ**失敗時こそ証拠を最大限保存する必要があります。**

`catch` でも、

```text
result: "error"

manifest.json
events.jsonl
summary.json
artifacts/screenshots/error.png
artifacts/playwright/trace.zip
```

を保存してください。

さらにBrowser Runnerで、

```ts
page.on("pageerror", ...)
page.on("console", ...)
```

などをObservationHubへ接続し、少なくともBrowser errorとPage errorをRunへ残したいです。現在のBrowser Runnerにはこの接続がありません。

ここは**P0**です。

---

### 残るP1：Scenarioの入力時刻がPhysics Step基準ではありません

Scenario定義自体は、

```ts
fromStep
toStep
```

となっています。

ところがBrowser Runnerでは、

```ts
((segment.toStep - segment.fromStep) / 60) * 1000
```

を計算して `page.waitForTimeout()` しています。つまり「Physics Step 240になったら左旋回」ではなく、「4秒待ったら左旋回」です。

通常ならかなり近くなりますが、Before側が重くAfter側が軽くなるような性能修正では、ブラウザのFrame timingによってPhysics Step数が少しずれる可能性があります。

今回のBefore/After思想なら、

```text
KeyW down
↓
physicsStep >= 240 まで待つ
↓
ArrowLeft down
↓
physicsStep >= 480 まで待つ
↓
ArrowLeft up
```

の方が厳密です。

Agent APIに、

```ts
getState().machine.physicsStep
```

または、

```ts
getPhysicsStep()
```

を追加し、PlaywrightはWall ClockではなくPhysics Step境界で入力を切り替えるのを勧めます。

これで、

**同じseed + 同じ初期状態 + 同じPhysics Stepで同じ入力**

になります。

---

### 残るP1：観測自体がFrame時間へ入り込んでいます

これは性能解析用基盤なので注意したい部分です。

現在、Observation実行時は毎Frame、

* `renderer.frame`
* `physics.machine.sample`
* `world.streaming.stats`
* `camera.follow.sample`

などを生成しています。さらに `physics.machine.sample` はMachineTelemetrySample全体をスプレッドコピーしています。

そして入力変化判定では毎Frame、

```ts
JSON.stringify(this.lastObservedControls)
JSON.stringify(this.observedControls)
```

を実行しています。

これは、元の設計で避けたかった「観測機構自体が測定対象のFrameへ負荷を足す」方向です。

特にScenarioにはすでに、

```ts
sampleEverySteps
```

がありますが、Browser側のMachine sample記録では現在使っていません。

ここは、

```text
renderer.frame
→ 毎Frame

Machine full state
→ sampleEverySteps

World full stats
→ 5～10Frameごと

Camera full state
→ 必要Scenarioでは毎Frame
→ その他は間引く

input.changed
→ JSON.stringifyせず各値比較
```

くらいにするとよいです。

Spike発生時だけ前後Windowを高密度で残す、という最初の方針とも合います。

---

### GPU timingがSummaryまで届いていません

`ObservationHub.recordFrameTiming()` は `gpuMs` を扱える設計ですが、Engineから呼ぶ現在のコードでは、

```text
rafMs
physicsMs
renderMs
streamingCommitMs
```

だけを渡しています。したがって`buildRunSummary()`が参照する `renderer.frame.data.gpuMs` は通常nullのままになります。

Agent `getState()` ではRendererのGPU Sampleをすでに取得できる構造なので、最新の有効なGPU Sampleを `recordFrameTiming()` に渡せばよいです。

これでBefore/Afterの、

```text
CPUは改善した
GPUは悪化した
```

といった切り分けもできます。

---

### 11 Scenarioの「仕組み」はできましたが、自動テストはまだ1件です

Browser Runner自体は共通Scenario定義を受け取るため、11件を実行できる設計になっています。これは良いです。

ただし現在のPlaywrightテストは、11 Scenarioが登録されていることを確認した後、実際にPLAYしているのは `starter-plane-turn-left` だけです。

元の受け入れ条件を厳密に採るなら、最低でも別のEnd-to-Endテストで11件を順番に、

```text
run
↓
assertion
↓
events
↓
終了
```

まで通す必要があります。

毎回フル30秒でContinuous Integrationが重すぎるなら、

```text
通常CI
→ 各Scenarioの短縮smoke版

nightly / manual
→ 完全な11 Scenario
```

でもよいと思います。

---

### Playerへの変更は観測モードだけに限定した方が安全です

Playerは今回、

```ts
await engine.load(loadProject(localStorage) ?? p);
```

へ変わっています。つまりAgentモードだけでなく通常のPlayer起動でもlocalStorageのProjectを自動ロードするようになりました。

Browser RunnerからPlayerへProjectを注入するために必要なのは分かりますが、観測基盤を追加した結果としてProduction Playerの通常挙動まで変わります。

例えば、

```ts
?agent=1 / ?observe=1
```

の場合だけlocalStorage Projectを読むか、

```text
専用 observation project injection
```

を用意する方が安全です。

これはP2ですが、直すのは簡単です。

## 【注意点・例外】

今回のDiffで、前回指摘した本質的な設計問題はほぼ解消されています。特に「Node simulatorだけをAI観測と呼んでいる」「非同期Workerを同期化している」「Worldイベントを推測している」という問題はなくなりました。

したがって、**作り直す必要はありません。** 今回の `fa30268` をベースに、失敗Run保存、Step同期、観測オーバーヘッド削減を追加する方針で十分です。