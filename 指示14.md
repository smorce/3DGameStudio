# 3DGameStudio: 非同期World Streaming / Frame Budget / Worker Pipeline 大改修

対象Repository:
https://github.com/smorce/3DGameStudio

Base branch:
fix/streaming-frame-budget

新しい作業Branchを作成してください。

推奨Branch名:
perf/async-world-streaming-frame-budget

既存branchへ直接追加実装せず、必ず上記Baseから新しいbranchを作ってください。

# 今回の設計方針

| 領域 | 現在 | 今回の目標 |
|---|---|---|
| 物理更新 | 1/60秒固定 | 維持 |
| 描画 | previous/current補間 | 維持 |
| 回転 | Quaternion補間 | 維持 |
| カメラ | 経過時間基準 | 維持 |
| Origin Rebase | Engineで一括処理 | 維持 |
| Chunk生成 | 同期経路あり | Worker化 |
| Chunk読込 | 必要になってから処理 | 未来位置を予測してPrefetch |
| Three.js Chunk追加 | 一括発生可能 | Frame Budget内で分割 |
| Physics Collider | Step付近で同期作成 | 到達前に準備 |
| LOD | 毎Frame root.traverse | 対象管理＋分散更新 |
| 性能評価 | Chunk生成中心 | Frame全体を計測 |

上記表は今回の設計契約です。
「維持」とした5項目は壊さず、「変更」とした6項目を今回の主対象としてください。
もしかしたら現在のブランチは少し修正が既に入ってるかもしれません。
コミット: 71d8e23 — Chunk Streamingを時間分散し、境界通過のFrame Spikeを抑える

# 0. 今回の目的

飛行機の離陸前、および旋回中に目視できるカクつきが残っています。

すでに以下は実装済みです。

- Physics Fixed Step = 1/60
- accumulator方式
- previous/current Physics Pose
- Render Interpolation
- Quaternion補間
- dt基準Camera Follow
- Shadow Follow
- Engine TransactionによるOrigin Rebase
- Rebase時のPhysics / Renderer / WorldRuntime同期
- Hinge anchor error計測

これらは原則として維持してください。

今回の主目的は、

「World Streamingに伴う重い仕事をMain Thread上の単一Frameへ集中させない」

ことです。

最終的に、概念上は次の構造にしてください。


Input
  │
  ▼
┌─────────────────────────────────────────┐
│ Main / Animation Loop                   │
│                                         │
│  input                                  │
│                                         │
│  Streaming Request / Prefetch           │
│                                         │
│  accumulator += elapsed time            │
│                                         │
│  while accumulator >= 1/60              │
│      Physics Critical Ready Commit      │
│      Physics Fixed Step                 │
│      World Rebase判定                   │
│      previous ← current                 │
│      current  ← physics pose            │
│                                         │
│  alpha = accumulator / fixedDt          │
│                                         │
│  interpolate(previous,current)          │
│        │                                │
│        ├── Machine Render               │
│        ├── Camera Follow                │
│        └── Shadow Follow                │
│                                         │
│  Streaming General Commit               │
│      └─ Main Thread時間予算内だけ処理    │
│                                         │
│  Render                                 │
└─────────────────────────────────────────┘
                  ▲
                  │ Ready Chunk Queue
                  │ Transferable ArrayBuffer
         ┌────────┴─────────────────┐
         │ Web Worker Pool          │
         │                          │
         │ Terrain生成               │
         │ Entity生成                │
         │ Mesh配列生成              │
         │ Normal計算                │
         │ Color配列生成             │
         │ Render Chunk準備          │
         └──────────────────────────┘


PhysicsだけはColliderが存在しない地面へ侵入してはいけないため、
Physics用ChunkのReady CommitだけはFixed Step境界で扱って構いません。

ただし、

「Main ThreadでChunkそのものを同期生成する」

ことは通常Play中は禁止します。


# 1. 最初にBaselineを保存する

変更前に必ず以下を実行してください。

pnpm typecheck
pnpm lint
pnpm format:check
pnpm test
pnpm test:integration
pnpm test:e2e
pnpm smoke
pnpm build

現在のRuntime挙動についてもEvidenceを保存してください。

特に以下を測定してください。

- frameTimeMs
- physicsStepMs
- renderMs
- streamingRequestMs
- streamingCommitMs
- renderChunkCommitMs
- physicsChunkCommitMs
- workerGenerationMs
- workerQueueLength
- workerInFlightCount
- readyRenderChunkCount
- readyPhysicsChunkCount
- chunksRequestedThisFrame
- chunksCommittedThisFrame
- renderChunksCommittedThisFrame
- physicsChunksCommittedThisFrame
- syncGenerationFallbackCount
- frame > 16.67ms 回数
- frame > 33.3ms 回数
- frame > 50ms 回数
- p50 frame time
- p95 frame time
- p99 frame time
- max frame time

performance.now() を使用してください。

Long Animation Frames APIが利用可能なブラウザでは、
PerformanceObserverによる long-animation-frame も追加計測して構いません。

ただし非対応ブラウザでも動作させてください。

Baseline Evidence:

docs/evidence/async-streaming-before.json


# 2. 現在の同期Streaming経路を洗い出す

以下を最低限確認してください。

packages/world-system/src/runtime.ts
packages/world-system/src/streaming.ts
packages/world-system/src/chunks.ts

packages/world-generator/src/index.ts
packages/world-generator/src/worker.ts

packages/renderer-three/src/index.ts
packages/renderer-three/src/instances.ts

packages/physics-rapier/src/index.ts

packages/engine-core/src/index.ts

現在の hot path から、

- generateChunk
- getChunk
- meshFor
- runtimeChunkMesh
- BufferGeometry生成
- computeVertexNormals
- Entity instance生成
- Collider生成

がどのタイミングで同期実行されているかを整理してください。

特に「1 ChunkのgenerateChunkが速い」だけで性能判断しないでください。

1 Frame全体で、

13 Chunk追加
+
Three.js Geometry生成
+
Normal生成
+
Entity生成
+
Rapier Collider生成

などが重なった場合の総時間を測ってください。


# 3. WorldRuntimeを非同期Request型へ変更する

現在のWorldRuntimeは同期的な getChunk() を中心にしています。

PLAY hot pathでは、これを使わない構造へ変更してください。

概念API例:

requestChunk(key, priority)
requestChunks(consumer, requests)

peekChunk(key)
peekPreparedChunk(key)

isPending(key)
isReady(key)

takeReadyRenderChunks(...)
takeReadyPhysicsChunks(...)

release(...)

ensurePhysicsReady(...)

実際の名前は既存設計に合う形で構いません。


重要:

同じChunkをRenderer / Physics / Editorが要求しても
Worker Jobを重複させないでください。

Chunkごとに概念的に、

ABSENT
QUEUED
GENERATING
READY
COMMITTED
EVICTABLE

相当の状態を管理してください。


# 4. Workerを本当に動かす

現在の packages/world-generator/src/worker.ts にある
handleWorkerRequest / generateChunkOnThisThread だけで終わらせず、
ブラウザの実Web Workerを使用してください。

必要なら以下を追加してください。

packages/world-generator/src/worker-entry.ts
packages/world-system/src/chunk-worker-pool.ts

など。


Worker Poolを用意してください。

初期値の目安:

workerCount =
clamp((navigator.hardwareConcurrency ?? 4) - 2, 1, 4)

ただし設定可能にしてください。

低性能端末でWorkerを増やしすぎないでください。


# 5. Workerが返すもの

Three.js Object3DやBufferGeometry自体をWorker/Main Thread間で送らないでください。

純粋なDataだけを返します。

推奨:

PreparedChunk {
  key
  chunkX
  chunkZ
  size
  resolution

  heights: Float32Array

  positions: Float32Array
  normals: Float32Array
  colors: Float32Array
  indices: Uint32Array

  entities

  generationTiming
}

可能であればGeometryはChunk-local座標にしてください。

例:

positions:
0 ... chunkSize

ChunkのScene位置:
chunkOrigin - worldOrigin

この方式にすればOrigin Rebaseで頂点配列を作り直す必要がなく、
Chunk Group / Collider Translationだけを変更できます。


# 6. Normal計算もWorkerへ移す

現在Main Thread側で

geometry.computeVertexNormals()

している処理は、Procedural TerrainについてWorker側へ移してください。

Worker側でnormal arrayを完成させ、

geometry.setAttribute(
  "normal",
  new THREE.BufferAttribute(normals, 3)
)

するだけにしてください。

Main ThreadではTerrain Normalの再計算をしないでください。


# 7. 色変換もWorker側へ移す

現在のようにMain Threadで大量の

new THREE.Color(hex)

や

flatMap()

を行う経路をProcedural Chunkのhot pathから外してください。

Worker側でRGB Float32Arrayまで生成してください。

Main Threadは完成済みのTyped ArrayをThree.jsへ渡すだけにします。


# 8. Transferable ArrayBufferを使用する

Worker → Main Threadで、

positions.buffer
normals.buffer
colors.buffer
indices.buffer
heights.buffer

など、大きなTyped Arrayのunderlying ArrayBufferをTransferしてください。

postMessage時にtransfer listを使ってください。

不要なStructured Cloneによる巨大Arrayコピーを避けてください。

転送後にWorker側でそのArrayBufferを再利用しないよう注意してください。


# 9. Main Thread Commit Queueを作る

Workerから返ったChunkを、
受信した瞬間にSceneへ追加しないでください。

Ready Queueへ入れてください。

例:

RenderReadyQueue
PhysicsReadyQueue

Main ThreadはFrameごとに時間予算内だけCommitします。


初期設定:

STREAMING_COMMIT_BUDGET_MS = 2.0

MAX_RENDER_CHUNKS_PER_FRAME = 2

MAX_PHYSICS_CHUNKS_PER_FIXED_STEP = 必要最小限

として開始してください。

設定可能にしてください。


概念:

const deadline = performance.now() + commitBudgetMs;

while (
    readyQueue.length &&
    performance.now() < deadline &&
    committed < maxChunks
) {
    commitOneChunk();
}


ただしFrameがすでに遅れている場合は
Render Chunk CommitをSkip可能にしてください。


# 10. Physics Chunkを特別扱いする

Physics Colliderが存在しない場所へ
Vehicleが侵入する設計は不可です。

Physics用Chunkについては、

REQUEST
↓
WORKER READY
↓
FIXED STEP境界でCOMMIT
↓
その後physics.step()

にしてください。

Main ThreadでTerrainを生成し直してはいけません。


Play開始時
Respawn時
Teleport時
Course Start変更時

には、

ensurePhysicsReady(position)

のようなBarrierを用意してください。

必要なPhysics Chunkが揃うまで、
Simulation開始を待って構いません。

UIには既存の「準備中…」相当を使ってください。


通常走行中はBarrierを発生させず、
十分早くPrefetchして間に合わせるのが目標です。


# 11. Terrain ColliderもChunk-local化を検討する

可能であればPhysics Terrainも、

Collider vertices = chunk local
Collider translation = chunkOrigin - worldOrigin

としてください。

これにより、

- Worker生成結果をRebaseと独立できる
- RenderとPhysicsで同じChunk-local概念を使える
- Rebase時にColliderのtranslationだけを変更できる

ようにします。

ただし既存テスト・衝突精度を壊す場合は、
まず非同期化を優先し、この変更を独立させても構いません。


# 12. Streaming Priorityを実装する

最低でも以下のPriorityを用意してください。

P0: Physics Critical
P1: Physics Prefetch
P2: Visible Renderer
P3: Renderer Prefetch
P4: Editor / Background

同じChunkへ複数Priority要求が来たら、
最も高いPriorityへ昇格してください。

Priority Queueで処理してください。

FIFOだけにしないでください。


# 13. 先読み方式を改善する

現在のように、

velocity direction
→ round()
→ 少数Chunkをaheadへ追加

だけに依存しないでください。

飛行ゲームとして、
複数未来位置を予測してください。

初期値:

t = 0.5s
t = 1.0s
t = 1.5s
t = 2.0s

position + velocity * t

を計算し、
各予測位置周辺のChunkをPrefetchしてください。


さらに、

現在方向
+
直前Frameまで要求していたPrefetch領域

のUnionを一定期間維持してください。

旋回時に、

左方向を要求
右方向を要求
左方向を要求
右方向を要求

のようにChunk Queueが振動しないようにしてください。

方向切替に余裕を持たせる仕組みを入れてください。

Retentionの初期目安:

0.5～1.0秒

または

unloadRadiusの外へ十分出るまで

とします。


# 14. Renderer StreamingとPhysics Streamingを分ける

考え方:

Physics:
狭い
高Priority
必ず先に準備

Renderer:
広い
低Priority
数Frame遅れてもよい


Rendererが遅れても、
Physicsを止めないでください。

Physics用TerrainをRenderer完成待ちにしないでください。


# 15. Builtin Entity Templateを共有する

Procedural Chunkごとに、

tree
rock
building

のGeometry / Material Templateを新規作成しないでください。

Builtin TemplateをRenderer単位またはAssetTemplatesで共有してください。

Chunkごとに必要なのは、
基本的にInstance Matrixだけにしてください。

Geometry / Materialを再生成しないでください。

Dispose時に共有Resourceを二重disposeしないよう、
所有権を明確にしてください。


# 16. LOD更新をroot.traverseから外す

現在の毎Frameの

root.traverse(...updateLod...)

をHot Pathから外してください。

LOD更新が必要なWorldAssetBatchなどを
専用Collectionで管理してください。

例:

Set<WorldAssetBatch>

または

Set<LodUpdatable>


さらに、遠距離LOD判定は必ずしも毎Frame必要ありません。

Frame分散して構いません。

例:

frameIndex % 4

などでグループを分ける、
または時間予算内だけ評価してください。


# 17. Animation Loopの基本構造

Physics Fixed Stepは維持してください。

PHYSICS_FIXED_DT = 1 / 60

accumulator方式も維持してください。

概念:

frame(now) {
    const dt = clamp(...);

    collectInput();

    streaming.updateRequests(...);

    accumulator += dt;

    while (accumulator >= fixedDt) {
        streaming.commitPhysicsCriticalReady();

        physics.step();

        processOriginRebase();

        previous = current;
        current = physics.renderState();

        accumulator -= fixedDt;
    }

    alpha = accumulator / fixedDt;

    const interpolated =
        interpolate(previous, current, alpha);

    renderer.updateMachine(interpolated);
    renderer.updateCamera(dt);
    renderer.updateShadow(dt);

    streaming.commitGeneralWithinBudget();

    renderer.render();
}


Physics固定更新の中で、

- Terrain生成
- Entity生成
- Geometry生成
- Normal生成

を行わないでください。


# 18. requestAnimationFrame / setAnimationLoop

今回の本質はStreaming Hitch解消です。

Three.jsのsetAnimationLoopへ変更する場合は、
Engine/CoreがThree.jsへ直接依存しないよう
RendererAdapter境界を守ってください。

この変更は必須ではありません。

既存requestAnimationFrameを維持しても構いません。

Streaming非同期化・Frame Budgetを優先してください。


# 19. sampleHeightの同期生成を禁止する

重要です。

sampleHeight() のような問い合わせ関数が、
Cache Miss時に暗黙にFull Chunk生成を行ってはいけません。

PLAY hot pathでは、

Cacheあり:
cached heightsからsample

Cacheなし:
deterministicな軽量sampleGeneratedHeight等を使用

としてください。

「高さを1点知りたいだけなのにFull Chunk生成」
を起こさないでください。


# 20. Sync Fallbackを監視する

同期生成Fallbackを残す場合は必ず計測してください。

stats:

syncGenerationFallbackCount

正常な通常Playでは、

Play開始時のWarmup完了後:
syncGenerationFallbackCount == 0

を目標にしてください。

通常飛行中のChunk Boundary通過で
syncGenerationFallbackCountを増やさないでください。


# 21. Worker JobのCancel / Stale対策

World reload
Project reload
Play停止
Runtime dispose
Seed変更
Preset変更

などが起きた場合、
古いWorker ResultをSceneへCommitしてはいけません。

現在のgeneration/ticket概念を維持または強化してください。

request:

runtimeGeneration
jobId
chunkKey

responseにも同じ情報を含め、

generationが古ければdiscardしてください。


dispose時には、

- Worker terminate
- Queue clear
- Ready result release
- Pending Promise解決/Reject
- Geometry cleanup

を行ってください。


# 22. Cache上限を維持する

非同期化した結果、
無制限にChunkを保持しないでください。

既存cacheLimit / ref-count / evictionの考え方を維持してください。

以下を別々に監視してください。

generated cache
prepared cache
committed renderer
committed physics
worker in-flight


# 23. Frame Profilerを実装する

Runtime Statsへ以下を追加してください。

Frame
- current
- p50
- p95
- p99
- max

Physics
- lastMs
- maxMs

Render
- lastMs
- maxMs

Streaming
- requestMs
- workerGenerationMs
- commitMs
- renderCommitMs
- physicsCommitMs

Queue
- queued
- inFlight
- readyRender
- readyPhysics

Chunk
- requested
- generated
- committedRender
- committedPhysics
- cancelled
- syncFallback


直近600～1200 Frame程度のring bufferで十分です。

Debug HUDで確認可能にしてください。


# 24. Long Animation Frame計測

ブラウザが

PerformanceObserver.supportedEntryTypes.includes(
  "long-animation-frame"
)

を満たす場合だけLong Animation Frameを収集してください。

非対応時は何もしないでください。

これは補助Telemetryです。

ゲーム動作をこのAPIへ依存させないでください。


# 25. 性能テスト

今回一番重要です。

以下のScenarioを用意してください。


Scenario A: 離陸

Starter Plane
Airfield

0 → 150m

Chunk Boundary:
32m
64m
96m
128m

を通過する。


Scenario B: 長距離直進

500m以上飛行

複数Origin Rebaseを通過。


Scenario C: 旋回

300m前後から旋回し、
320m Chunk Boundary付近を通過する。

左右両方で試す。


Scenario D: Car

Procedural Grasslandを長距離走行。


Scenario E: Boat

Procedural Archipelagoを長距離航行。


各Scenarioで、

- Frame Timing
- Streaming Timing
- Queue
- Chunk Commit
- Worker Timing
- Sync Fallback
- Rebase
- Hinge Error

を記録してください。


# 26. 性能Acceptance Criteria

CIではマシン性能差があるので、
ハードウェア依存の絶対Frame Timeを
厳格なUnit Testにしすぎないでください。

代わりに構造上の条件を自動Testしてください。


必須:

- PLAY中、Main ThreadでgenerateChunkしない
- Worker Job deduplicate
- Priority昇格が動く
- stale resultを破棄する
- Ready Commitがbudgetを超えて無制限に処理しない
- 1Frameで13 Render Chunkを一括commitしない
- Chunk Boundaryでsync fallbackしない
- normal/color生成がWorker側
- Typed Array BufferをTransferする
- Physics CriticalがRendererより高Priority
- Respawn時に必要Physics ChunkがReadyになってからSimulation再開
- Origin Rebase連続性維持
- Hinge anchor error < 0.03m
- Air-stop 0
- NaN / Infinity 0
- Existing finite worldを壊さない


ローカルPerformance Target:

60Hz環境を想定して、

- p95 frame time <= 16.7～20ms程度を目標
- p99の大きなSpikeを削減
- 33ms超Frameを大幅削減
- Streaming起因50ms超Frame = 0を目標

としてください。

ただしこれらはEvidenceとして記録し、
CIの不安定な絶対Fail条件にはしないでください。


# 27. Scheduler Unit Test

Fake Clockを使ってください。

例:

budget = 2ms

1 Chunk commit cost =
mock 0.8ms

なら、

同一Frameで2Chunkだけcommitし、
3個目を次Frameへ残す

ことを検証してください。

実時間performance.now()へ依存するUnit Testにしないでください。


# 28. Worker Determinism Test

同一GeneratorInputについて、

sync generateChunk()
worker-side pure generate function

の結果が一致することを確認してください。

最低限:

- heights
- entity ids
- positions
- rotations
- scales
- indices count

を比較してください。


# 29. Render Prepared Data Test

Workerで生成された、

positions
normals
colors
indices

について、

- finite
- expected length
- normals finite
- normal lengthが異常でない
- vertex/index範囲正常

をTestしてください。


# 30. Streaming Boundary Regression

32m Chunkの場合、

x/z = 31.9
32.1

のような境界を行き来させても、
同じChunkのgenerate/cancel/generateを高速に繰り返さないこと。

Retention / hysteresisが効くことを検証してください。


# 31. Existing Runtime Stability Testを維持

既存の、

- Origin Rebase continuity
- Starter Plane >500m
- multiple rebases
- Hinge anchor error
- world position continuity
- Shadow follow
- Builtin collider
- Car
- Boat
- Plane
- finite world

テストは削除しないでください。

Thresholdを緩めて通すことも禁止します。


# 32. Evidence

以下を生成してください。

docs/evidence/async-streaming-before.json
docs/evidence/async-streaming-after.json

docs/evidence/async-streaming-flight.json
docs/evidence/async-streaming-turn.json

必要なら:

docs/evidence/async-streaming-frame-spikes.json


After Evidenceに最低限:

{
  gitHead,
  branch,
  workingTreeDirty,

  frame: {
    p50Ms,
    p95Ms,
    p99Ms,
    maxMs,
    over16_7,
    over33_3,
    over50
  },

  physics: {...},

  render: {...},

  streaming: {
    workerCount,
    generated,
    cancelled,
    syncFallbackCount,
    maxQueueLength,
    maxReadyQueueLength,
    maxWorkerGenerationMs,
    maxCommitMs
  },

  flight: {
    distanceM,
    rebaseCount,
    airStopSteps,
    maxHingeAnchorErrorM
  },

  tests: {...}
}

を保存してください。


# 33. Documentation

以下を更新してください。

docs/procedural-world-architecture.md

追加:

## Async World Streaming

以下を図付きで記載。

Worker Pool
↓
Generation Queue
↓
Prepared Chunk Cache
↓
Render / Physics Ready Queue
↓
Frame-Budgeted Commit


さらに、

## Frame Lifecycle

Input
↓
Streaming Request
↓
Fixed Physics
↓
Rebase
↓
Interpolation
↓
Streaming Commit
↓
Render

を書いてください。


docs/runtime-stability-report.md
KNOWN_ISSUES.md

も必要に応じて更新してください。


# 34. やってはいけないこと

以下は禁止します。

- Physics Fixed Stepを可変dtへ戻す
- Interpolationを削除する
- Origin RebaseをPhysics.step内へ戻す
- Chunk Radiusを極端に減らして誤魔化す
- Geometry品質を極端に落として誤魔化す
- Physics Colliderを遅延させて地面抜けを許す
- Streaming HitchをCamera smoothingで隠す
- Worker化したと言いながらgenerateChunkだけWorkerにして、
  Main Threadで重いNormal/Color生成を残す
- Worker Result受信時に全Ready Chunkを即commitする
- 13 Chunkを同一Frameで一括commitする
- sync fallbackを無計測で残す
- test thresholdを緩めてPASSさせる
- Existing test削除
- Starter Planeだけの特殊条件
- setTimeoutで見た目だけ遅らせる
- 無制限Worker生成
- 無制限Cache


# 35. 実装優先順位

次の順番で進めてください。

Phase 1
Frame Profilerを入れてBefore計測

Phase 2
Prepared Chunkのpure data構造を作る

Phase 3
Worker Entry / Worker Poolを実装

Phase 4
Transferable Buffer対応

Phase 5
WorldRuntimeをRequest / Ready型へ変更

Phase 6
RendererをReady Queue + Frame Budget型へ変更

Phase 7
Physics Critical Ready Queueを導入

Phase 8
Future-path Prefetch + Retentionを導入

Phase 9
Builtin Template / LOD hot path最適化

Phase 10
E2E / Performance Evidence


一度に全部書いてからTestするのではなく、
PhaseごとにTestを追加してください。


# 36. 最終検証

必ず実行してください。

pnpm typecheck
pnpm lint
pnpm format:check
pnpm test
pnpm test:unit
pnpm test:integration
pnpm test:e2e
pnpm smoke
pnpm telemetry:plane
pnpm build


# 37. 完了報告

最後に以下を報告してください。

1. Branch名
2. Final commit SHA
3. Beforeのボトルネック
4. 実際に最も時間を使っていた処理
5. Architecture変更
6. Worker構成
7. Worker数
8. Transferable対象
9. Streaming Priority
10. Commit Budget
11. Prefetch方式
12. Physics safety設計
13. Frame p50 / p95 / p99 / max Before → After
14. >16.7ms Frame数 Before → After
15. >33.3ms Frame数 Before → After
16. >50ms Frame数 Before → After
17. sync fallback Before → After
18. Chunk Boundary結果
19. 旋回試験結果
20. Starter Plane長距離結果
21. Car / Boat regression結果
22. 全Test結果
23. 変更ファイル一覧
24. 残Known Issues


# 最重要な完成条件

単に「Workerを入れた」では完成ではありません。

完成条件は、

「Chunk Boundaryや旋回によって複数Chunkが必要になっても、
重い仕事が単一のMain Thread Frameへ集中しない」

ことです。

そして通常Playでは、

Main Thread:
Physics
Interpolation
Small Commit
Render

だけを安定して実行し、

Terrain / Entity / Mesh Data / Normal / Colorの重い生成は
Worker側で先に完了させてください。