# 3DGameStudio AI観測・再現・検証基盤 実装指示

対象リポジトリ:

`https://github.com/smorce/3DGameStudio`

対象ブランチ:

`perf/async-world-streaming-frame-budget`

今回の目的は、単純にログを増やすことではありません。

**AIエージェントが、自分でゲームを観察し、問題を再現し、内部状態を調べ、原因を絞り込み、修正後に同じ条件で再実行し、Before / Afterを比較できる環境を作ってください。**

考え方としては次のループを成立させます。

```text
問題を指定
  ↓
再現シナリオを起動
  ↓
実際にゲームを動かす
  ↓
画面・物理・描画・ワールド・入力・カメラ等を観察
  ↓
CLIからログを検索・集約
  ↓
原因候補を絞る
  ↓
コードを修正
  ↓
同じseed・同じ開始状態・同じ入力で再実行
  ↓
Before / Afterを比較
  ↓
改善・悪化・不変を判定
```

このループを、今後CodexやGPT-6 Astraなどのコーディングエージェントが自律的に利用できる形にしてください。

---

# 0. 作業開始時の必須事項

まず対象ブランチを最新状態で確認してください。

コードを変更する前に必ず以下を読んでください。

* ルートの `AGENTS.md`
* `docs/runtime-telemetry.md`
* `docs/testing.md`
* `docs/performance.md`
* `packages/runtime-telemetry`
* `packages/engine-core`
* `packages/world-system`
* `packages/physics-rapier`
* `packages/renderer-three`
* `playwright.config.ts`
* 既存の `scripts/*probe*`
* 既存の `scripts/*telemetry*`
* 既存の診断クラス
* `tests/e2e`
* `tests/integration`
* `tests/unit`

さらにリポジトリ全体を検索して、現在存在する以下を洗い出してください。

```text
console.log / warn / error / debug
debugEvents
RuntimeTelemetry
FrameProfiler
SpikeDiagnostics
TurnMotionDiagnostics
CameraFollowToggleLog
GPU timer
stats()
exportJson / exportJsonLines
writeFile / writeFileSync
docs/evidence への書き込み
独自Ring Buffer
独自dump / export
Performance計測
World Streaming計測
Origin Rebase計測
Playwright trace / screenshot
```

最初に「現在どこで何を記録しているか」を把握してから変更してください。

既存ログを機械的に削除してはいけません。

---

# 1. 最重要方針

現在の `packages/runtime-telemetry` を捨てたり、別の競合するTelemetry基盤を新設したりしないでください。

**既存RuntimeTelemetryを中核として拡張してください。**

ただし現在の `MachineTelemetrySample` は互換性を維持してください。

既存テスト、HUD、Physics、既存解析スクリプトを壊すようなフィールド意味変更は禁止します。

必要であれば新しい上位概念として、

```text
TelemetryHub
ObservationHub
RuntimeObservability
```

などを導入して構いませんが、

```text
既存 RuntimeTelemetry
        ↓
新しい共通観測基盤
```

という関係にしてください。

既存基盤を置換するのではなく包含してください。

---

# 2. 観測する情報を4種類に分ける

全情報を同一巨大JSONへ押し込まないでください。

共通基盤では次の4種類を明確に扱います。

```text
State
Event
Metric
Artifact
```

意味は次のとおりです。

```text
State
  ゲームの現在状態・連続状態

Event
  何かが発生した瞬間

Metric
  時間、件数、負荷などの数値

Artifact
  screenshot、Playwright trace等の大きな成果物
```

例:

```text
State
- machine position
- machine rotation
- velocity
- camera position
- world origin
- current chunk

Event
- play started
- respawn
- chunk queued
- chunk ready
- chunk committed
- chunk evicted
- origin rebase
- collision
- camera mode changed
- frame spike

Metric
- RAF time
- physics time
- render time
- GPU time
- streaming commit time
- draw calls
- triangles
- worker queue
- cache hit rate

Artifact
- screenshot
- Playwright trace
- error snapshot
```

---

# 3. 共通Run Contextを導入する

すべての観測データを「どの実験で発生したのか」追跡できるようにしてください。

最低限、各実行に以下を持たせます。

```ts
interface ObservationRunManifest {
  schemaVersion: 1;

  runId: string;
  scenarioId: string;

  startedAt: string;
  completedAt?: string;

  gitCommit: string;
  gitBranch: string;
  workingTreeDirty: boolean;

  seed?: number;

  scenarioDefinitionHash?: string;

  environment: {
    mode: "browser" | "node";
    browser?: string;
    viewport?: [number, number];
    devicePixelRatio?: number;
  };
}
```

`runId` は必須です。

すべてのState / Event / Metric / Artifactを同じ `runId` に関連付けてください。

---

# 4. 共通イベントスキーマ

少なくとも以下の情報を持つ共通イベント形式を実装してください。

```ts
interface TelemetryEvent {
  schemaVersion: 1;

  runId: string;
  scenarioId?: string;

  timestampMs: number;

  frame?: number;
  physicsStep?: number;

  source:
    | "engine"
    | "physics"
    | "renderer"
    | "world"
    | "camera"
    | "input"
    | "browser"
    | "test"
    | "diagnostic";

  type:
    | "event"
    | "metric"
    | "state"
    | "assertion";

  name: string;

  entityId?: string;
  chunkKey?: string;
  correlationId?: string;

  data: Record<string, unknown>;
}
```

必要なら型安全性のためDiscriminated Unionへ発展させて構いません。

ただし、

```text
runId
timestamp
source
type
name
```

は共通軸として保持してください。

---

# 5. イベント命名規則

名前は検索しやすい階層形式に統一してください。

例:

```text
engine.play.started
engine.play.stopped
engine.respawn

input.changed

physics.machine.sample
physics.contact.started
physics.contact.ended

world.chunk.queued
world.chunk.generating
world.chunk.ready
world.chunk.committed
world.chunk.evicted

world.origin.rebase.started
world.origin.rebase.completed

renderer.frame
renderer.gpu.frame
renderer.resource.changed

camera.follow.mode_changed

diagnostic.frame.spike
diagnostic.turn.verdict

test.assertion.passed
test.assertion.failed
```

既存実装と照らして名称は調整して構いませんが、同じ意味のログ名が複数乱立しないようにしてください。

---

# 6. 既存診断機能を共通基盤へ統合する

最低限、現在存在する以下を統合してください。

```text
RuntimeTelemetry
FrameProfiler
SpikeDiagnostics
TurnMotionDiagnostics
CameraFollowToggleLog
WorldRuntime.stats()
WorldRuntime.debugEvents
Origin Rebase計測
Renderer stats
GPU timer
Worker Pool stats
Playwright assertion
```

重要:

既存の `SpikeDiagnostics` 等が持つ専用Ring Bufferや専用解析ロジックは、意味があるなら維持して構いません。

無理に一つの汎用クラスへ潰さないでください。

ただし最終的な出力と検索経路は共通観測基盤へ接続してください。

例えば、

```text
SpikeDiagnostics
      ↓
diagnostic.frame.spike
      ↓
ObservationRun
```

という形にします。

---

# 7. ホットパスを重くしない

今回の観測機能そのものがフレーム落ちを起こしてはいけません。

特に以下では同期ファイル書き込みを行わないでください。

```text
requestAnimationFrame
Physics fixed step
Renderer render
World streaming commit
Origin Rebase
```

通常の記録はMemory Ring Bufferを中心にしてください。

大量ログを毎フレームファイルへflushしないでください。

推奨:

```text
通常
  ↓
Ring Buffer

異常発生
  ↓
前後Windowを保持

Run終了
  ↓
まとめてexport
```

Browser実行では、必要ならPlaywright側またはCLI側が最後にデータを回収してファイルへ保存してください。

---

# 8. 異常前後の自動保存

既存SpikeDiagnosticsの発想を一般化してください。

例えば、

```text
20ms超
  → spike candidate

33.3ms超
  → 詳細Window保存

50ms超
  → 高優先度異常
```

のように、異常発生前後を残せるようにします。

閾値は現在の既存診断との互換性を優先してください。

最低限、

```text
異常前
異常フレーム
異常後
```

の情報を一つの関連Windowとして検索できるようにしてください。

---

# 9. Run Storage

ローカル実験結果は次へ集約してください。

```text
.observability/
└─ runs/
   └─ <runId>/
      ├─ manifest.json
      ├─ events.jsonl
      ├─ metrics.jsonl
      ├─ states.jsonl
      ├─ summary.json
      └─ artifacts/
         ├─ screenshots/
         └─ playwright/
```

実装上必要なら分割構造は微調整して構いません。

ただし、

```text
1 run = 1 directory
```

という考え方は守ってください。

`.observability/` は通常Git管理対象外にしてください。

---

# 10. docs/evidenceの役割を変更する

現在の `docs/evidence` を生の実験ログ置き場として増殖させないでください。

今後は、

```text
.observability/runs
    = 生の実験結果

docs/evidence
    = 人間が採用した比較結果・証拠
```

と役割を分離してください。

既存 `docs/evidence` を大量削除する必要はありません。

後方互換性を壊さず、新規実行結果からこの方針へ移行してください。

---

# 11. CLIを実装する

エージェントがGUIを介さず観測結果を調査できるCLIを作ってください。

入口は必ず、

```bash
pnpm observe
```

に統一します。

最低限、次を実装してください。

```bash
pnpm observe scenarios

pnpm observe run <scenario>

pnpm observe summary <runId|latest>

pnpm observe query <runId|latest>

pnpm observe frame <runId|latest> <frame>

pnpm observe compare <beforeRunId> <afterRunId>

pnpm observe explain <runId|latest>
```

---

# 12. CLI: scenarios

```bash
pnpm observe scenarios
```

ですべての再現シナリオを表示してください。

Machine-readable出力も用意してください。

例:

```bash
pnpm observe scenarios --json
```

---

# 13. CLI: run

```bash
pnpm observe run starter-plane-turn-left
```

で、そのシナリオを実際に実行してください。

終了時には最低限、

```text
runId
scenario
pass/fail
duration
summary path
artifact path
```

を表示してください。

`--json` ではエージェント向けJSONだけを標準出力できるようにしてください。

---

# 14. CLI: query

次のような検索を可能にしてください。

```bash
pnpm observe query latest --source world

pnpm observe query latest --name world.chunk.committed

pnpm observe query latest --type metric

pnpm observe query latest --frame 1842

pnpm observe query latest \
  --name diagnostic.frame.spike \
  --before-ms 500 \
  --after-ms 500
```

完全なデータベースは不要です。

初期実装はJSON Linesをストリーム読み込みして検索する形でも構いません。

重要なのはAIが巨大ログ全体を読まず、必要部分だけ取り出せることです。

---

# 15. CLI: summary

```bash
pnpm observe summary latest --json
```

で、AIがまず読むべき短いSummaryを返してください。

例:

```json
{
  "runId": "...",
  "scenario": "world-streaming-fast-flight",
  "result": "fail",

  "frame": {
    "p50Ms": 16.4,
    "p95Ms": 18.1,
    "p99Ms": 39.7,
    "maxMs": 52.2
  },

  "spikes": {
    "over20Ms": 12,
    "over33Ms": 3,
    "over50Ms": 1
  },

  "streaming": {
    "maxWorkerQueue": 6,
    "maxCommitMs": 29.1
  },

  "notableEvents": [
    "diagnostic.frame.spike",
    "world.origin.rebase.completed"
  ]
}
```

---

# 16. CLI: compare

同じScenarioのBefore / Afterを比較してください。

```bash
pnpm observe compare BEFORE AFTER --json
```

最低限比較するもの:

```text
pass/fail
frame p50/p95/p99/max
spike counts
physics time
render time
GPU time
streaming commit
worker queue
sync fallback
draw calls
triangles
rebase
scenario固有metrics
assertion結果
```

同一ScenarioでないRunを比較した場合は、通常は警告または失敗にしてください。

必要なら `--force` を設けて構いません。

---

# 17. CLI: explain

```bash
pnpm observe explain latest --json
```

を実装してください。

ここでは外部LLMを呼ばないでください。

決定的なルールベース解析にします。

例:

```json
{
  "suspects": [
    {
      "name": "world.streaming.commit",
      "confidence": 0.91,
      "evidence": {
        "frame": 1842,
        "rafMs": 48.2,
        "streamingCommitMs": 31.4,
        "renderMs": 7.1,
        "physicsMs": 2.8
      }
    }
  ]
}
```

AIエージェントはこれを読んで、次に詳細ログやコードを調査します。

---

# 18. Browser Agent Observation API

開発・テスト時だけ、ゲーム内部状態をPlaywrightやAIエージェントから読めるAPIを公開してください。

名称:

```ts
window.__MACHINE_STUDIO_AGENT__
```

最低限:

```ts
interface MachineStudioAgentApi {
  version: 1;

  getState(): AgentGameState;

  getTelemetrySummary(): unknown;

  queryRecent(filter?: AgentEventFilter): unknown[];

  getRunInfo(): unknown;

  listScenarios(): unknown[];

  resetScenario(): Promise<void>;
}
```

必要なら安全なScenario操作APIを追加して構いません。

---

# 19. Browser APIの安全条件

このAPIはProduction向け公開インターフェースにしないでください。

通常Productionでは `window` に取り付けないでください。

有効化条件を、

```text
development
test
明示的query flag
明示的environment flag
```

等に限定してください。

また、基本は読み取り専用にしてください。

ゲーム内部オブジェクトを自由に書き換える汎用APIは作らないでください。

許可する操作は、

```text
scenario reset
scenario load
diagnostic toggle
```

など、用途が限定されたものだけにしてください。

---

# 20. AgentGameState

最低限以下を一度に取得できるようにしてください。

```text
engine
- mode
- frame

machine
- id
- position
- simulationPosition
- worldOrigin
- rotation
- velocity
- speed
- grounded state
- controls

camera
- position
- target
- follow mode

world
- current chunk
- loaded render chunks
- loaded physics chunks
- ready queues
- generation queue
- worker queued
- worker inflight
- cache
- rebase count

renderer
- draw calls
- triangles
- geometries
- textures
- GPU timing

frame
- RAF time
- CPU work time
- physics time
- render time
- streaming request
- streaming commit
```

取得できない値は推測せず `null` またはavailability情報を使ってください。

---

# 21. Scenarioを共通データとして定義する

Scenario定義をCLI用とPlaywright用で二重管理しないでください。

単一のScenario definitionを、

```text
CLI
Playwright
agent API
```

から共通利用してください。

Scenarioには最低限、

```ts
interface ObservationScenario {
  id: string;
  description: string;

  seed?: number;

  setup: ...;

  timeline: ...;

  assertions: ...;

  tags: string[];
}
```

相当の情報を持たせてください。

---

# 22. 初回実装で必ず作る11シナリオ

今回は最初から以下をすべて実装してください。

```text
starter-plane-straight
starter-plane-takeoff
starter-plane-turn-left
starter-plane-turn-right

world-streaming-forward
world-streaming-fast-flight
world-streaming-turn

origin-rebase-200m
origin-rebase-flight

camera-follow-turn
terrain-entry
```

「後で追加する」として省略してはいけません。

---

# 23. starter-plane-straight

Starter Planeを安定した既知状態から開始します。

既存のStarter Plane設定、Control Binding、Physics経路を利用してください。

専用の偽Physicsやテスト専用移動処理を作ってはいけません。

目的:

```text
前進入力
↓
正方向へ直進
↓
機体状態と性能を観測
```

最低限確認:

```text
forwardSpeedの符号
world displacement
姿勢
速度
接地状態
Physics continuity
frame performance
```

意図しない大きな横移動、回転、停止があれば解析可能なログを残してください。

---

# 24. starter-plane-takeoff

Starter Planeの地上走行から安定離陸までを再現します。

既存に安定離陸判定ロジックが存在する場合は、それを再利用してください。

同じ意味の離陸判定を別実装しないでください。

記録対象:

```text
ground roll
liftoff
speed
vertical speed
grounded wheels
height above terrain
pitch
lift
drag
thrust
```

Scenario verdictで、

```text
stable takeoff detected
```

を機械判定できるようにしてください。

---

# 25. starter-plane-turn-left

Starter Planeを一定時間前進させ、その後左旋回を行います。

Control Bindingを調査し、実際のゲーム操作と同じ経路で操作してください。

記録:

```text
physics quaternion
render quaternion
angular velocity
interpolation alpha
camera target
screen movement
frame timing
```

既存TurnMotionDiagnosticsを活用してください。

旋回方向が想定と一致することも検証してください。

---

# 26. starter-plane-turn-right

`starter-plane-turn-left` と対になるScenarioです。

初期条件、速度、入力時間などは可能な限り左右対称にしてください。

これにより左だけ・右だけで発生する問題も比較可能にしてください。

---

# 27. world-streaming-forward

Procedural World上を前方向へ移動し、複数Chunk境界を通過させてください。

観測:

```text
chunk request
queue
generation
ready
commit
eviction
cache
worker queue
physics chunks
render chunks
sync fallback
frame timing
```

Chunk Lifecycleの流れを追えるようにしてください。

---

# 28. world-streaming-fast-flight

Starter Plane等の高速移動可能なMachineを使い、通常より速い速度でWorld Streaming領域を横断してください。

目的は、

```text
高速移動
↓
Chunk生成要求増加
↓
Worker / Ready Queue / Commit
↓
フレーム負荷
```

の因果関係を観察することです。

特に、

```text
worker queue
generation latency
render commit
physics commit
frame spikes
sync fallback
terrain availability
```

を残してください。

---

# 29. world-streaming-turn

ChunkをStreamingしながら旋回します。

単純直進だけでは発生しない、

```text
進行方向変更
Streaming範囲変更
Chunk request変更
Camera movement
Physics movement
```

を同時に観測します。

World StreamingとMotion Smoothnessの双方を記録してください。

---

# 30. origin-rebase-200m

Global座標で少なくとも約200m移動するScenarioを作ってください。

Origin Rebaseが実際に発生する条件を現在のコードから確認してください。

Scenario中に少なくとも1回のRebaseが発生するようにしてください。

記録:

```text
origin before
origin after
delta
global position
simulation position
physics pose
renderer pose
rebase total time
physics rebase time
renderer rebase time
runtime commit time
telemetry sync time
```

Rebase前後でGlobal座標上の移動が不連続になっていないことを検証してください。

---

# 31. origin-rebase-flight

飛行中にOrigin Rebaseを発生させてください。

Rebase前後で、

```text
velocity
orientation
altitude
global trajectory
joint state
camera tracking
```

が不自然に飛ばないことを確認してください。

Rebaseそのものの時間と、その前後のFrame Spikeも関連付けてください。

---

# 32. camera-follow-turn

旋回中のCamera Followを重点観測します。

既存 `CameraFollowToggleLog` と `TurnMotionDiagnostics` を再利用してください。

最低限、

```text
vehicle render position
camera position
camera target
vehicle-to-target distance
vehicle screen delta
interpolation alpha
physics step count
RAF
```

を関連付けます。

Production既定Camera Followの挙動を必ず記録してください。

既存の比較診断モードが利用できるなら、その情報もScenario summaryに含めてください。

---

# 33. terrain-entry

未ロードまたはこれからPhysics準備されるTerrain領域へMachineを進入させます。

目的:

```text
Terrain準備
↓
Physics Collider準備
↓
Machine進入
```

の順序を検証することです。

確認:

```text
terrain availability
physics chunk ready
collider availability
chunk lifecycle
machine position
height above terrain
ground contact
```

Terrain準備前にMachineが進入して落下する等の問題を検出可能にしてください。

---

# 34. Scenarioの再現性

各Scenarioについて可能な限り、

```text
同じseed
同じProject
同じ開始座標
同じ開始姿勢
同じ入力Sequence
同じDuration
```

で再現してください。

日時や乱数に依存する挙動を直接使わないでください。

ランダムが必要な場合はseedをManifestへ記録してください。

---

# 35. Screenshot / Playwright Artifact

すべてのフレームをScreenshot保存する必要はありません。

最低限、

```text
Scenario開始
重要Event
Assertion failure
大きなSpike
Scenario終了
```

のうち有用な箇所をArtifactとして保存できるようにしてください。

Playwright traceも必要に応じてRunへ紐付けてください。

ArtifactはEventから参照できるようにしてください。

---

# 36. AssertionもTelemetryへ流す

Playwright / Scenario assertionの結果も観測基盤へ記録してください。

例:

```text
test.assertion.passed
test.assertion.failed
```

data:

```json
{
  "assertion": "stable takeoff detected",
  "expected": true,
  "actual": false
}
```

これによりAIがテスト失敗と周辺ゲーム状態を同時に検索できるようにしてください。

---

# 37. エラーもRunに関連付ける

Run中の以下を可能な範囲で捕捉してください。

```text
uncaught error
unhandled rejection
browser console error
Playwright page error
worker error
```

ただし既存エラーハンドリングを壊さないでください。

同じエラーを大量に重複記録しない工夫もしてください。

---

# 38. Migration Inventoryを残す

今回の変更後、

```text
docs/observability.md
```

を作成してください。

最低限、

```text
全体Architecture
Run構造
Schema
CLI
Browser API
Scenario
Artifact
Performanceへの配慮
既存診断との関係
```

を記載してください。

さらに現在存在したログ／診断がどこへ統合されたか分かるMigration表を含めてください。

例:

```text
旧: SpikeDiagnostics
新: diagnostic.frame.spike

旧: WorldRuntime.debugEvents
新: world.*

旧: CameraFollowToggleLog
新: camera.follow.*
```

これにより統合漏れを確認できるようにしてください。

---

# 39. 既存API互換性

次を守ってください。

```text
MachineTelemetrySampleの意味を勝手に変更しない
既存RuntimeTelemetry APIを不用意に削除しない
既存HUDを壊さない
既存probeを即削除しない
既存testsを弱くしない
```

必要なら旧APIを新基盤のFacadeとして残してください。

---

# 40. テスト

新しい基盤そのものについてUnit Testを追加してください。

最低限:

```text
Ring Buffer
Run Context
Event schema
JSONL export
query
summary
compare
explain
scenario registry
```

Integration Testでは、

```text
Physics → Telemetry
World → Telemetry
Renderer/Engine → Telemetry
```

の主要経路を確認してください。

Playwrightでは11 Scenarioが実行可能であることを確認してください。

---

# 41. Performance検証

観測基盤追加前後で、既存Performance Probeまたは同等手段を使ってPerformanceを比較してください。

特に、

```text
frame p95
frame p99
max frame
physics
render
streaming commit
```

を確認してください。

Telemetry記録そのものが明らかなRegressionを起こした場合、そのまま完了扱いにしてはいけません。

ホットパスでの、

```text
JSON.stringify
巨大オブジェクトcopy
同期IO
無制限array push
```

は避けてください。

---

# 42. package.json

ルートに少なくとも、

```json
{
  "scripts": {
    "observe": "tsx scripts/observe/cli.ts"
  }
}
```

相当を追加してください。

既存scriptは、今回の変更だけを理由に削除しないでください。

---

# 43. 推奨ファイル構成

既存Architectureを調査したうえで調整して構いませんが、概念的には以下を想定しています。

```text
packages/runtime-telemetry/
└─ src/
   ├─ index.ts
   ├─ schema.ts
   ├─ run-context.ts
   ├─ events.ts
   ├─ query.ts
   ├─ summary.ts
   └─ compare.ts

packages/engine-core/
└─ src/
   └─ agent-observation.ts

scripts/
└─ observe/
   ├─ cli.ts
   ├─ runner.ts
   ├─ scenarios.ts
   └─ explain.ts

tests/
├─ unit/
├─ integration/
└─ e2e/
```

ただしファイルを不必要に細分化しないでください。

既存コード構成との整合性を優先してください。

---

# 44. 実装後の品質ゲート

最低限すべて実行してください。

```bash
pnpm format:check
pnpm typecheck
pnpm lint
pnpm test:unit
pnpm test:integration
pnpm test:e2e
pnpm build
```

さらに、

```bash
pnpm observe scenarios
```

で11 Scenarioが存在することを確認してください。

全Scenarioについて少なくとも1度実行し、Run directoryとSummaryが生成されることを確認してください。

---

# 45. 11 Scenarioの存在確認

最終確認時、次がすべて存在しなければ未完了です。

```text
starter-plane-straight
starter-plane-takeoff
starter-plane-turn-left
starter-plane-turn-right
world-streaming-forward
world-streaming-fast-flight
world-streaming-turn
origin-rebase-200m
origin-rebase-flight
camera-follow-turn
terrain-entry
```

---

# 46. End-to-End受け入れテスト

最終的に少なくとも次の操作が成立することを確認してください。

```bash
pnpm observe scenarios

pnpm observe run starter-plane-turn-left

pnpm observe summary latest --json

pnpm observe query latest \
  --name diagnostic.frame.spike

pnpm observe explain latest --json
```

続いてコードを変更しなくても、別Runを作り、

```bash
pnpm observe run starter-plane-turn-left
```

を再実行できること。

さらに、

```bash
pnpm observe compare <RUN1> <RUN2> --json
```

で同条件Runを比較できること。

ここまで成立して初めて今回の基盤完成とします。

---

# 47. AIエージェント利用を最優先する

出力は人間だけでなくAIが読むことを前提にしてください。

そのため、

```text
stable field names
stable event names
JSON output
small summaries
filterable logs
explicit units
explicit null
schema version
run id
scenario id
```

を重視してください。

CLIのJSONモードで装飾文字や進捗メッセージを混ぜないでください。

標準出力をJSONとして直接parseできる状態にしてください。

必要な診断メッセージは標準エラー出力へ分離してください。

---

# 48. やってはいけないこと

以下は禁止します。

```text
既存RuntimeTelemetryを全面置換する
既存Physics値の意味を変える
テスト専用の偽PhysicsでScenarioを成立させる
Scenarioごとに別々のLogging方式を作る
CLIとPlaywrightでScenario定義を二重管理する
毎フレーム同期ファイル書き込みする
巨大ログをAIへ丸ごと渡す前提にする
外部LLM APIを観測基盤の必須依存にする
Productionで自由な内部状態変更APIを公開する
既存テストを削除してGreenにする
失敗テストをskipして完了扱いにする
```

---

# 49. 実装方針について迷った場合

新しい抽象化を増やす前に、

```text
既存RuntimeTelemetry
既存diagnostics
既存stats
既存Playwright
既存probe
```

を再利用できないか確認してください。

今回の中心思想は、

**新しい診断機能を大量に横へ増やすことではなく、既にある観測能力を共通基盤へ集約し、AIがアクセスできる形にすること**

です。

---

# 50. 作業の進め方

設計提案だけ出して停止しないでください。

対象コードを調査したら、そのまま実装へ進んでください。

通常の細かな判断は既存Architectureから合理的に決めてください。

途中で別案を提案してユーザー判断待ちにしないでください。

重大な破壊的変更が避けられない場合のみ、その事実を明示してください。

---

# 51. 最終報告

完了時は次を報告してください。

```text
1. 実装概要
2. Architecture
3. 追加・変更ファイル
4. 既存ログのMigration状況
5. CLIコマンド一覧
6. Browser Agent API
7. 11 Scenarioそれぞれの内容
8. Unit / Integration / E2E結果
9. Performance比較
10. 残った制約
11. 実際に生成したRun例
12. Before / After compareの使用例
```

テストが失敗している場合は隠さず記載してください。

「実装したが未検証」は完成扱いにしないでください。

---

# 最終目標

この改修後は、AIエージェントに例えば、

```text
「飛行機が左旋回すると一瞬カクつく。原因を調べて修正して」
```

とだけ指示した場合でも、

```text
scenario選択
↓
run
↓
summary
↓
spike検索
↓
physics / render / streaming / camera比較
↓
原因箇所調査
↓
修正
↓
同一scenario再実行
↓
compare
↓
改善確認
```

まで、自力で進められる状態を目標としてください。

これは単なるLogging改修ではありません。

**3DGameStudioを「AI自身が観察・再現・検証できるゲーム開発環境」に変えるための基盤改修です。**

## 【注意点・例外】

今回、11シナリオはすべて初回実装の必須要件にしました。一方、各シナリオの物理パラメータや入力キーをプロンプト側で固定しすぎてはいません。現在のStarter PlaneやControl Bindingを実装AI自身に確認させ、**実ゲームと同じ入力経路を使う**よう指定しています。これにより、テストを通すためだけの別挙動を作るのを防ぎます。

また `explain` は意図的に大規模言語モデル非依存にしています。ここまでを決定的な診断ツールにしておけば、将来使うエージェントをGPT-6 Astra以外へ変更しても観測基盤をそのまま使えます。