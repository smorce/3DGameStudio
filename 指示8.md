# 実装依頼

対象リポジトリ:

https://github.com/smorce/3DGameStudio

対象ブランチ:

feat/starter-templates

このブランチの現在実装を必ず最初に調査したうえで作業してください。

今回の目的は、単発の飛行機デバッグではありません。

今後、車・飛行機・ボート・将来追加するマシンの挙動を数値で調査できる「走行テレメトリー基盤」を作り、その基盤を実際に使ってStarter Planeの離陸不安定問題を再現・分析・修正してください。

さらに、テレメトリーの速度情報を利用して、Play中の画面にゲームらしい速度表示を追加してください。

速度表示はデバッグUIではなく、プレイヤー向けの通常機能です。

# 基本方針

場当たり的な `console.log` を大量に追加して原因を見る方法にはしないでください。

物理エンジンが持っている実値を取得し、それを構造化された共通テレメトリーとして公開してください。

概念としては、

Physics
→ Runtime Telemetry
→ Logger / Tests / HUD

という一方向の構造にしてください。

ログ用、テスト用、画面表示用に別々の速度計算を実装してはいけません。

UI側でフレーム間の座標差から速度を推定する実装も避けてください。

RapierのRigidBodyが持つ線速度を正式な情報源にしてください。

# テレメトリー基盤

将来拡張できる小さな共通基盤を実装してください。

特定のStarter Planeだけに依存する構造にはしないでください。

最低限、Machine単位で次の情報を取得できるようにしてください。

時刻またはPhysics Step番号、Machine ID、Position、Rotation、Linear Velocity、Angular Velocity、全速度、水平速度、機体前方速度、垂直速度、Throttle、Steering、Brakeを扱ってください。

速度の単位は内部ではメートル毎秒としてください。

表示用にキロメートル毎時へ変換できるようにしてください。

さらに飛行問題を調べるため、取得可能な範囲でPitch、Yaw、Roll、総揚力、総抗力、総空力、総Thruster推力、Machine総質量、重力による重量を記録してください。

Panel空力では既に各Panelについてlift、drag、angleOfAttack、dynamicPressureなどを計算しているため、この値を計算後に捨てず、Machine単位の集計値を取得できる構造を検討してください。

必要なら詳細モードでPanel単位の値も取得可能にしてください。ただし通常モードではログ量を必要以上に増やさないでください。

着陸脚についてRapier 0.19.3の現在使用しているAPIから接地情報やサスペンション状態を安全に取得できる場合は記録してください。

利用できないAPIを推測で呼び出してはいけません。インストール済みRapierの型定義・実APIを確認してください。

接地情報を取得できない場合は、その事実を報告し、代替値を無理に「接地」と名付けないでください。

# 記録方式

Physicsは現在固定1/60秒で動作しているため、解析用テレメトリーはPhysics Stepを基準にしてください。

レンダリングフレームレートに依存させないでください。

長時間プレイでメモリを使い続けないよう、通常実行ではサイズ制限されたRing Bufferなどを使用してください。

テストでは完全な短時間ログをMemory上で取得できるようにしてください。

最低限、次の操作ができるAPIを用意してください。

記録開始、記録停止、クリア、現在値取得、記録済みSample取得、JSONまたはJSON Lines形式でのExport。

CSVが自然に追加できるなら対応して構いませんが、JSON系の構造化形式を優先してください。

ログ基盤そのものはゲーム挙動を変更してはいけません。

# 速度表示

Play中のゲーム画面に速度を表示してください。

これはデバッグパネルではなく通常HUDです。

表示例は、

SPEED
112 km/h

程度のシンプルなもので構いません。

現在のMachineのRigidBodyのWorld Space Linear Velocityの大きさを速度として使用してください。

speedMps = sqrt(vx² + vy² + vz²)

speedKph = speedMps × 3.6

としてください。

将来Windを実装した場合にAirspeedを別に追加できるよう、現在の値を内部API上で曖昧に `airspeed` と呼ばないでください。

`speedMps`、`worldSpeedMps` 等、意味の明確な名前にしてください。

HUDではキロメートル毎時を整数程度で表示してください。

Physics値そのものは平滑化してはいけません。

見た目が細かく揺れる場合だけ、HUD表示レイヤーで短い平滑化を行って構いません。

ログとテストでは常に生値を使ってください。

Play中だけ表示し、EDITでは邪魔にならないよう非表示にしてください。

車・飛行機・ボートのすべてで同じ速度計を利用してください。

特定テンプレート専用にはしないでください。

# Starter Planeの現在の問題

現在、Starter Planeを走らせると、

走行
→ 少し浮く
→ すぐ落ちる
→ 再び少し浮く
→ また落ちる

という挙動を何度か繰り返し、なかなか安定して離陸しません。

考えられる原因として、

走行距離不足、機体重量、Thruster推力不足、離陸速度付近での揚力不足、主翼迎角、Pitch不安定、着陸脚またはRaycast Vehicleのサスペンションによる上下振動、簡易空力モデルの特性

などがあります。

ただし、これらを最初から原因と決めつけないでください。

テレメトリーで判断してください。

# 現在のテストの問題

既存のStarter Plane離陸テストも確認してください。

現在のテストが単に

maxHeight - minHeight

のような一時的な高度上昇だけを「離陸」と見なしている場合、一瞬ジャンプしてすぐ着地する現象を成功扱いしてしまいます。

今回の現象を正しく検出できる回帰テストへ改善してください。

「一度浮いた」ことと「安定して離陸した」ことを区別してください。

# 実行手順

1. まず現在の `machine-system`、`physics-rapier`、`aerodynamics`、`engine-core`、Studio UI、既存テストを調査し、現状のデータフローを整理してください。

2. 汎用Runtime Telemetry基盤を設計・実装してください。Physics内部で取得した値をEngineから読み出せるようにし、UI・テスト・Loggerが同じSnapshotを利用できるようにしてください。

3. テレメトリー基盤自体のUnit Testを追加してください。速度計算、メートル毎秒からキロメートル毎時への変換、記録開始停止、Ring Buffer上限、Exportなどを検証してください。

4. Play画面へ速度HUDを実装してください。車、飛行機、ボートで共通利用できることを確認してください。HUDの値がPhysicsのテレメトリー値と一致する構造にしてください。

5. 修正前のStarter Planeを全開Throttle、Steering 0で十分な時間走らせ、Physics Step単位のログを取得してください。最低でもPosition、Velocity、Speed、Vertical Speed、Pitch、総Lift、総Drag、総Thrust、Machine Mass、Weightを時系列で確認してください。

6. 「安定離陸」を明示的に定義してください。例えば一定高度以上を一定時間連続で維持すること、離陸後の垂直速度や高度推移が安定していることなどを組み合わせ、一瞬のジャンプを成功扱いしない判定にしてください。地形からの高さを安全に取得できる場合は絶対Y座標だけよりGround Clearanceを優先してください。

7. 修正前ログから原因を特定してください。特に、Lift / Weight比が1付近を周期的に往復しているか、速度低下がLift低下より先に起きているか、Pitch振動がLift変動より先に起きているか、接地・サスペンション変動と上下運動が同期しているか、最高速度が離陸に必要な速度へ届いているかを調べてください。

8. 必要に応じて比較実験を行ってください。一度に複数条件を変えず、主翼角、Thruster推力、機体質量、翼面積、着陸脚条件などを1項目ずつ変更し、原因を切り分けてください。闇雲なパラメータ調整は禁止します。

9. 原因が分かったら、最小限の修正を実施してください。単にThrusterを極端に強くする、質量を極端に軽くする、重力を弱くする、といった問題を隠す修正は避けてください。物理モデル、機体形状、主翼角、重心、着陸脚、安定性など、本当の原因に対応してください。

10. 修正後に同じテレメトリー試験を再実行し、修正前と修正後を定量比較してください。速度、高度、Pitch、Vertical Speed、Lift / Weight、離陸開始時刻、離陸開始速度、離陸後1秒・2秒の高度などを比較してください。

11. 車とボートの既存挙動を壊していないことを確認してください。既存のUnit、Integration、End-to-End、Build、Typecheck、Lintなど、リポジトリで定義されている品質ゲートを実行してください。

12. 最後に、原因、証拠、実施した修正、修正前後の数値、変更ファイル、残る制約をレポートしてください。

# 原因判定の考え方

例えば、速度は十分あるのにPitchが上下へ振れ、その直後にLiftが大きく上下しているなら、単純な推力不足よりPitch安定性を疑ってください。

速度がある値を超えた瞬間だけLift / Weight > 1となり、浮いた直後に速度低下して再び1未満になるなら、離陸速度の境界条件、抗力、迎角、推力余裕を疑ってください。

離陸前から機体Y位置が周期的に動き、車輪またはサスペンション状態と同期するなら、着陸脚側を疑ってください。

全開でも速度が十分伸びず、ThrustとDragがほぼ釣り合っているなら、推力または抗力側を疑ってください。

最高速度でもLiftがWeightへ届かないなら、重量、翼面積、迎角、揚力モデルを疑ってください。

必ず時系列上の「どの変化が先に起きたか」を見て原因を判断してください。

# テスト改善

従来の最大高度だけを見るテストは残す必要がなければ置き換えてください。

Starter Planeについて、少なくとも次の性質をテストしてください。

十分な加速後に離陸すること。

一瞬だけ浮いて着地する状態を成功扱いしないこと。

離陸後に一定時間、高度を維持または上昇できること。

異常なPitch Oscillationが発生しないこと。

弱いThrusterでは標準機より離陸性能が低下すること。

翼面積を減らすと離陸性能が低下すること。

速度HUD用の速度とPhysics Telemetryの速度が同じ情報源であること。

# 実装上の制約

既存Schemaや保存データへ不要なTelemetry履歴を保存しないでください。

走行ログはRuntime情報であり、通常のProject保存データとは分離してください。

UIのためにPhysicsへReact依存を入れてはいけません。

PhysicsのためにUI依存を入れてはいけません。

テレメトリー取得を理由にPhysics結果を変更してはいけません。

60Hzで大量のReact State更新を行って性能を落とさないでください。

詳細ログを常時無制限保存しないでください。

既存のFixed Step、Rapier、Panel空力、Machine構造を理解せずに全面書き換えしないでください。

# ドキュメント

テレメトリー基盤の設計、取得できる項目、単位、記録周期、Buffer制限、Export形式をドキュメント化してください。

速度HUDで表示する速度の定義も明記してください。

また、Starter Planeについて、

症状
→ テレメトリーで観測した事実
→ 原因
→ 比較実験
→ 修正
→ 修正後結果

という順で調査レポートを残してください。

# 完了条件

「ログ基盤を追加した」だけでは完了ではありません。

「飛行機を調整した」だけでも完了ではありません。

以下すべてを満たして完了です。

汎用テレメトリー基盤が存在する。

ログを構造化して取得・Exportできる。

車・飛行機・ボートから利用できる。

Play画面に速度が表示される。

速度HUDとログが同じPhysics値を利用する。

Starter Planeの問題を新しいログで再現できる。

数値的根拠から原因を説明できる。

原因に対応した修正が入っている。

一瞬のジャンプでは通らない離陸Regression Testがある。

修正前後を数値比較している。

既存機能のRegressionがない。

関連ドキュメントと最終報告を残している。

# 最終報告で必ず示すもの

最終回答では、実装内容だけでなく、「最初に何が原因だと仮定したか」ではなく「ログから実際に何が原因だと判明したか」を明確にしてください。

修正前と修正後について、可能なら表形式で、

離陸開始速度、離陸開始時間、最高速度、最大高度、離陸後の最低高度、最大Pitch、Pitch振動幅、Lift / Weight比、前進距離

を比較してください。

推測で埋めてはいけません。

取得できなかった値は取得できなかったと明記してください。

最後に、今回直していない既知の制約も隠さず記載してください。