# 実装依頼

あなたは 3DGameStudio の実装担当です。

対象リポジトリ:
https://github.com/smorce/3DGameStudio

対象ブランチ:
feat/generic-flight-controls

今回の目的は、個別テンプレートの座標を場当たり的に調整することではありません。

「Part同士の接続」「編集時の接地」「Play時のWheel/Suspension物理」
について共通ルールを整理し、現在発生している複数の小さな配置バグを根本から修正してください。


# 最重要方針

今回の変更では、次の原則を3DGameStudio全体の基本仕様として扱ってください。

## 原則1: 接続されたPart同士に理由のない空白を作らない

Panel / Block / Wheel / Suspension / Motor / Steering / Hinge /
Thruster、および今後追加されるPartを含めて、

「接続した2つのPartの間に、座標補正による見えない空間が存在する」

状態を原則禁止します。

これはPanel→WheelやPanel→Suspensionだけの特例ではありません。

全Part共通のAttachmentルールとして実装してください。


基本的な不変条件は、

    親Connectorのワールド座標
        ==
    子Connectorのワールド座標

です。

許容誤差は自動テスト上 1e-3 m 以下を目安としてください。

接続面を持つPartでは、単に原点同士を一致させるのではなく、
「親側の取付面」と「子側の取付面」を一致させます。


## 原則2: 見えないオフセットを禁止する

以下のような実装は避けてください。

- 理由なく child size / 2 を足す
- Wheelだから固定値を0.65m下げる
- テンプレート専用の謎のYオフセットを加える
- commit後にConnectorを書き換えることで辻褄を合わせる
- Rendererだけ位置を補正してPhysicsとの差をごまかす

距離が必要なら、なぜその距離が存在するのかをデータモデル上明示してください。

例:

Panel
  ↓ 密着
Suspension upper mount
  ↓
見えるSuspensionの伸縮部分
  ↓
Wheel axle

これは許可します。

一方、

Panel
  ↓
何も存在しない0.3m
  ↓
Wheel

は禁止です。


## 原則3: 機構として必要な距離は許可する

以下は「空白」ではありません。

- Suspensionのストローク
- Motorのシャフト
- Hingeの回転構造
- 将来追加されるSpring / Spacer / Rope等の実体を持つ距離

ただし、距離が存在する理由がPartまたは機構として明示されている必要があります。


# 現在確認できている不具合

以下をすべて修正してください。

1. Part同士を接続すると謎の空白が発生する場合がある
2. PanelとWheelの間に空白ができる
3. PanelとSuspension上端の間に空白ができる
4. 編集画面でWheelが地面に埋まる
5. Play開始後、飛行機の左右Main Gearの片側Suspensionが極端に縮む
6. そのSuspensionがWheel内部まで完全に入り込む
7. 見た目だけでなく機体自体が左右に傾く
8. EditとPlayでWheel/Suspensionの位置基準が一致していない


# 調査対象

最低限、以下を確認してください。

packages/machine-system/src/index.ts

- placementConnectors()
- attachmentPoint()
- findAttachmentCandidates()
- commitAttachment()
- connectTemplateParts()
- attachTemplateSuspensionWheel()
- wheelSuspensionSettings()
- carTemplate()
- starterCarTemplate()
- planeTemplate()
- compileMachine()

packages/renderer-three/src/index.ts

- syncPartVisualTransforms()
- render()
- WheelのEdit時位置
- SuspensionのPlay時表示更新

packages/renderer-three/src/part-visuals.ts

- createSuspensionVisual()
- updateSuspensionVisual()
- createWheelVisual()

packages/physics-rapier/src/index.ts

- DynamicRayCastVehicleController生成部分
- addWheel()
- Wheel connection point
- suspension rest length
- suspension stiffness
- compression
- relaxation
- max force
- poses()
- telemetry

tests/unit/placement.test.ts

tests/e2e/play-stop-edit-pose.spec.ts

および関連するPlane / Vehicle / Physics / Rendererテスト。


# 実装方針A
# Attachment計算を一本化する

現在の

- findAttachmentCandidates()による通常配置
- connectTemplateParts()によるテンプレート配置
- commitAttachment()内の追加補正

で、接続位置計算が別々にならないようにしてください。

共通のAttachment Transform計算関数を導入してください。

概念的には、

    solveAttachmentTransform(
      parentTransform,
      parentConnector,
      childTransform,
      childConnector
    )

のような責務です。

この関数は、

    world(parentConnector)
      == world(childConnector)

となるchild Transformを返してください。


重要:

parent connectorのnormalを使って

    normal * childSize / 2

を後から加えるような方式ではなく、

最初から「子Partの実際の取付Connector」を正しい位置に定義し、
そのConnector同士を一致させてください。


# 実装方針B
# Connectorを「実際の取付位置」として定義する

PartごとにConnectorの意味を明確にしてください。

例:

Panel
- edge
- face
- wheel mount等

Block
- 実際の表面

Wheel
- axle / mount

Suspension
- suspension-input = upper mount
- suspension-wheel = lower/axle side

Motor
- fixed mount
- output side

Hinge
- fixed input
- rotating output

Thruster
- fixed mount

などです。

名前は既存仕様との互換性を優先してください。

不要なスキーマ変更は避けて構いません。


# 実装方針C
# commitAttachment()後にConnector位置を変更して辻褄を合わせない

可能な限り、

Attachment候補を計算する前と、
commitした後で、
同じConnector定義を使用してください。

現在のように、

候補位置を決める
↓
Partを移動する
↓
Connector位置を書き換える

という流れが必要でない設計へ寄せてください。

既存保存データとの互換処理が必要なら、
配置計算本体とは分離してください。


# 実装方針D
# Wheelを直接接続した場合とSuspension経由を区別する

Wheelには少なくとも次の2種類があります。

A:
Panel / chassis
↓
Wheel

B:
Panel / chassis
↓
Suspension
↓
Wheel

この2つを同じ「0.65mの見えないSuspension」として扱わないでください。


## A: Wheel直接接続

明示的なSuspension Partが存在しない場合、

見えない0.65mの空白を追加してはいけません。

Wheelの取付位置はAttachment Connectorによって決めてください。


ただしRapierのRaycast Vehicle Controllerに必要な内部サスペンション値については、
見た目のAttachment距離とPhysics内部値を混同しないでください。

必要であれば、

- visual attachment position
- physics raycast suspension parameters

を明確に分離してください。


## B: Suspension経由

Panelの接続面とSuspension上端を密着させます。

Wheelとの距離は、
Suspension機構の現在長によって決まります。

つまり、

Panel
=============
[Suspension upper mount]
        |
        | actual suspension length
        |
      [Wheel]

となるようにしてください。


# 実装方針E
# Edit画面でWheelを地面に埋めない

現在、Edit画面でWheelが地面へ埋まるケースがあります。

RendererだけWheelを上へ移動する修正は禁止します。

Attachment関係を維持したまま接地させてください。


原則:

Wheelの下端が地形より下にある場合、
Wheelだけを上げない。

接続されたMachine全体、または必要な接続Assembly全体を、
必要量だけ上方向へ移動してください。


平坦な初期地形なら概念的には、

    lowestWheelBottomY =
        min(wheelCenterY - wheelRadius)

    lift =
        groundY - lowestWheelBottomY

    if lift > epsilon:
        translate whole machine by +lift Y

です。


ただし地形システムに既存のheight queryがある場合は、
固定のgroundY=0へ決め打ちせず既存APIを使用してください。


重要:

Wheelだけを移動してAttachmentを壊さないこと。


# 実装方針F
# Play中のSuspension異常圧縮をPhysics側で修正する

現在の飛行機ではPlay中、
左右Main Gearの片側SuspensionがWheel内部まで完全に潰れ、
機体そのものも傾くケースがあります。

これはRendererだけ直してはいけません。


Rapier DynamicRayCastVehicleControllerには、

- setWheelMaxSuspensionTravel()
- wheelSuspensionLength()
- wheelSuspensionForce()
- wheelIsInContact()
- wheelSuspensionRestLength()

等があります。

これらを適切に使用してください。


## 最大ストローク

setWheelMaxSuspensionTravel()を設定してください。

固定値を無根拠にハードコードして終わらせず、
SuspensionSettingsへ追加すべきか、
restLengthから導出すべきか、
既存保存データとの互換性を含めて検討してください。

必要ならSchemaを拡張して、

    maxTravel

のような設定を追加して構いません。

ただし既存Projectが読み込めなくならないよう、
デフォルト値を用意してください。


## 安全条件

SuspensionがWheel内部まで潰れて見えるような長さを
Physics側で許可しないこと。

最低長についても、
Wheel半径やSuspension Visual寸法との関係を確認してください。


# 実装方針G
# 左右対称性を「静止・対称条件」で保証する

すべての走行状態で左右Suspension長を一致させる必要はありません。

旋回中や不整地では左右差は正常です。


保証したいのは、

- 平坦地
- 左右対称なMain Gear
- 操作入力なし
- 静止または十分に静定した状態

です。

この条件では、

main-left
main-right

がほぼ同じ状態になる必要があります。


最低限確認する値:

- wheelSuspensionLength()
- wheelSuspensionForce()
- wheelIsInContact()
- wheelMaxSuspensionTravel()
- wheelSuspensionRestLength()


左右で、

- 一方だけ完全圧縮
- 一方だけ非接地
- 一方だけ異常な荷重
- 数フレーム後に機体が大きくRoll

という状態を禁止してください。


# 実装方針H
# RendererはPhysicsを正確に表示するだけにする

Play時のSuspension表示を、
Renderer独自の推測値でPhysicsから乖離させないでください。

可能ならPhysics側から、

- Wheel pose
- actual suspension length
- suspension direction

をRendererが使用できる形で渡してください。

既にRapierから

    wheelSuspensionLength(i)

を取得できるため、
これをSuspension Visualの現在長の一次情報として利用する設計を検討してください。


Renderer側で、

wheel.position - suspension.position

の距離だけから実サスペンション長を推定する処理は、
Physics上の定義とずれる可能性があるため整理してください。


# テスト要件

今回、Visualだけ直してテストを通す実装は禁止します。

Unit / Physics / E2Eを組み合わせてください。


## Test 1
## 全ConnectionのConnector一致

代表的なすべての組み合わせについて、

    distance(
      world(parentConnector),
      world(childConnector)
    ) < 1e-3

を確認してください。

最低限:

- Panel → Panel
- Panel → Block
- Panel → Wheel
- Panel → Suspension
- Panel → Motor
- Panel → Hinge
- Panel → Thruster
- Block → compatible Part
- Suspension → Wheel
- Hinge → compatible Part

を含めてください。


## Test 2
## 理由のない空白がない

Attachment直後に、
接続面間の距離が0に近いことを確認してください。

特定のテンプレート座標に依存するテストではなく、
共通Attachment invariantとして書いてください。


## Test 3
## Edit画面のWheel接地

Starter Car
Plane

について、

Edit画面初期状態で、

    wheelBottomY >= terrainHeight - epsilon

となること。

また、

Wheelを接地させるために、
Wheelと親PartのAttachmentが壊れていないこと。


## Test 4
## Edit → Play → Stop

Play前後でProjectデータが壊れず、
Stop後に正しいEdit Attachment Poseへ戻ること。

既存の

tests/e2e/play-stop-edit-pose.spec.ts

を新しい仕様へ更新してください。

既存テストにある

    wheelY = savedY - 0.65

のような仕様固定が不要になるなら、
正しい新仕様に更新してください。


## Test 5
## Plane Main Gear静的左右対称

Planeを平坦地に置き、
入力なしで一定時間Physicsを進めます。

main-left
main-right

について、

- 両方が正常に接地
- suspensionLengthが異常に潰れない
- maxTravelを超えない
- 左右長の差が小さい
- 左右荷重が極端に乖離しない

ことを確認してください。

閾値はPhysicsの揺らぎを考慮し、
過剰に厳密な完全一致にはしないでください。


例:

    abs(left.length - right.length) < reasonableTolerance

としてください。

許容値の根拠をコメントに残してください。


## Test 6
## SuspensionがWheelに埋没しない

Playを数秒進めても、
Suspensionの実長が設計上の最低値を下回らないこと。

Visual Screenshotだけで判定せず、
Physicsの数値で検証してください。


## Test 7
## 機体が静止時に異常Rollしない

平坦地、
入力なし、
左右対称Main GearのPlaneについて、

一定時間経過後のrollが小さい範囲に収まること。

ただし空力やPhysicsの微小誤差を考えて、
ゼロ完全一致にはしないこと。


# Templateについて

starterCarTemplate()
planeTemplate()

に座標を直接足して問題を隠さないでください。

テンプレート固有の設計値は残して構いませんが、

「空白を消すためにPlaneだけ-0.31mする」

のような修正は禁止です。

共通Attachment計算を修正した結果、
テンプレートも正しくなる状態を目指してください。


# 既存互換性

既存Projectデータを可能な限り維持してください。

placementConnectors()で古い保存データを補完している既存方針も確認してください。

Schema変更が必要なら、

- optional
- default
- migration相当の互換処理

を用意してください。


# 禁止事項

以下は禁止です。

- Rendererだけ位置をずらして見た目を直す
- Planeだけ特別なオフセットを足す
- 片側Main Gearだけ座標を変更する
- Wheelを地面から出すためWheel単体だけ上へ動かす
- Suspension Visualを短くしてPhysics異常を隠す
- maxSuspensionTravelを設定せず描画だけClampする
- テスト期待値を現在のバグに合わせる
- テストを削除して通す
- arbitrary sleepだけに依存する不安定なE2Eテスト
- 「だいたい見た目がよい」で完了扱いする


# 完了条件

以下をすべて満たしたら完了です。

1.
全PartのAttachmentが共通ルールで計算される。

2.
理由のない見えない空白がなくなる。

3.
Panel / Wheel / Suspension固有の例外処理だけに依存しない。

4.
Edit画面でWheelが地面へ埋まらない。

5.
Edit時の接地補正でAttachment関係を壊さない。

6.
Planeの左右Main Gearが平坦地・静止条件で正常に接地する。

7.
Play中にSuspensionがWheel内部まで完全圧縮しない。

8.
片側だけの異常圧縮によってPlaneが傾く問題をPhysics側から解消する。

9.
Rendererは修正後のPhysics状態を正確に描画する。

10.
Edit → Play → Stopで座標が飛ばない。

11.
既存のCar / Plane / Boat / free-buildに不要なRegressionを起こさない。

12.
Unit / Physics / E2Eテストを追加・更新し、今回の問題を再現できるRegression Testを残す。


# 実装手順

いきなり変更を始めず、まず現状コードを確認してください。

Phase 1:
現在のAttachment計算、Wheel Pose、Suspension Physicsのデータフローを整理する。

Phase 2:
今回の不具合を自動テストで再現する。
特にPlaneのmain-left/main-rightのPhysics状態を数値で確認する。

Phase 3:
共通Attachment solverを実装する。

Phase 4:
Wheel / SuspensionのEdit時位置を修正する。

Phase 5:
Rapier側にSuspension travel制約を実装する。

Phase 6:
Physics → RendererのSuspension状態同期を整理する。

Phase 7:
全Unit / E2E / Physicsテストを実行する。

Phase 8:
実際のPlaneテンプレートとStarter CarをPlayし、
数値Telemetryと画面の両方を確認する。


# デバッグ時に記録する値

Plane Main Gearについて最低限、

role
wheel index
connection point
restLength
maxTravel
current suspensionLength
suspensionForce
inContact
wheel radius
wheel world position
suspension upper world position

を確認してください。

特に、

main-left
main-right

を同じフレームで比較してください。


# 最終報告

完了後は以下の形式で報告してください。

## 原因
今回の複数バグがなぜ発生していたか。

## 設計変更
Attachment、Wheel、Suspensionをどのように整理したか。

## Physics修正
Rapier側で何を変更したか。

## Renderer修正
表示をPhysicsへどう同期させたか。

## 変更ファイル
ファイルごとの変更内容。

## Regression Tests
追加・変更したテストと、それぞれ何を保証するか。

## テスト結果
実行したコマンドと結果。

## 実測結果
Planeのmain-left/main-rightについて、
静止時の

- suspensionLength
- suspensionForce
- inContact
- roll

を示してください。

## 残る制約
今回の修正後も残る既知の制約。

「修正しました」だけで終了せず、
今回定義した不変条件がテストで保証されていることを示してください。