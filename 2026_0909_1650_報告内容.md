指示3.md の実装を完了しました。完成条件25項目すべて PASS、判定は COMPLETE です。

指定ブランチ `feat/world-runtime-foundations` の最新コミット `04d34f0` を取得・確認し、同じコミットから始まる `feat/machine-placement-system` で作業しました。詳細は実装報告書 (docs/placement-report.md)にまとめています。

LEVEL 1では、8種類のパーツを**「選んで、光る場所を押す」**操作で取り付けられます。パレット選択時はProjectを変更せず、配置確定時に接続と自動設定をまとめて作成します。配置後は候補を消し、追加したパーツだけを選択します。回転、つけ直し、コピー、削除、取消、半透明プレビュー、キーボード・タッチ操作も対応しました。

状態遷移は次のとおりです。

- IDLE → パーツ選択 → 候補表示 → `part.attach` → SELECTED
- SELECTED → つけ直し候補表示 → `part.reattach` → SELECTED
- 取消 → 候補消去・元パーツ保持

候補は `machine-system.findAttachmentCandidates()` が純粋計算します。親子の接続先、位置、回転、接続軸、接続種別を持ち、使用済み位置を除外します。親の移動・回転・拡大にも対応しました。Connectorにはoptionalな互換性情報を追加し、旧JSONを維持しています。Rendererは渡された候補の表示と選択通知を担当します。

Commandは `part.attach`、`part.reattach`、`part.turn` を追加しました。配置・つけ直し・回転はそれぞれ1回のUndoで戻り、Redoで同じID・位置・接続・設定を復元します。既存の `part.add` と詳細編集用Commandも残しています。

Wheelは前輪／後輪、駆動／操舵を自動判定します。新規Wheelの初期値はrevolute接続、friction 1.2、motorTorque 180、steering 0.45などを設定し、既存Raycast Vehicleで実際に前進することを確認しました。これらの専門値はLEVEL 1に表示しません。

変更ファイルは以下です。生成ログを含む一覧は実装報告書 (docs/placement-report.md)にも収録しています。

| 対象 | ファイル |
| --- | --- |
| 候補・互換性・回転計算 | `packages/machine-system/src/index.ts`、`packages/machine-system/src/math.ts` |
| Command・Schema | `packages/command-system/src/index.ts`、`packages/project-schema/src/index.ts` |
| Renderer・UI | `packages/renderer-three/src/index.ts`、`packages/ui-easy/src/index.tsx`、`apps/studio/src/main.tsx`、`apps/studio/src/style.css` |
| Unit・Integration | `tests/unit/placement.test.ts`、`tests/integration/placement.test.ts` |
| E2E | `tests/e2e/placement.spec.ts`、`tests/e2e/placement-helpers.ts`、`tests/e2e/studio.spec.ts`、`tests/e2e/flows.spec.ts` |
| 操作説明・制約 | `README.md`、`QUICKSTART.md`、`KNOWN_ISSUES.md`、`.prettierignore` |
| 設計・進捗 | `docs/placement-system.md`、`docs/placement-report.md`、`docs/machine-system.md`、`docs/testing.md`、`docs/progress.md` |

最終テスト結果です。実測ログと合否 (docs/evidence/placement-quality-gates.json)を保存しました。

| チェック | 結果 | 内容 |
| --- | --- | --- |
| Typecheck | PASS | workspace全体 |
| Lint | PASS | ESLint・循環依存なし |
| Unit | PASS | 45件 |
| Integration | PASS | 20件 |
| E2E | PASS | Chromium 16件 |
| Smoke | PASS | 11件 |
| Build | PASS | Studio・Player・Server |
| Format | PASS | Prettier |

完成条件の照合結果です。個別25項目の表は報告書 (docs/placement-report.md)にあります。

| Requirement | Status | Evidence |
| --- | --- | --- |
| パレット選択では追加せず、Placement Modeへ入る | PASS | E2Eのパーツ数・保存モデル比較 |
| 純粋な候補計算とRendererの責務分離 | PASS | 候補Unit・汎用表示API |
| Wheelの4候補と使用済み位置の除外 | PASS | 4→3→2→1→0のE2E |
| Snap・Connection・Wheel自動設定 | PASS | Command Unit・実走Integration |
| 配置後の候補消去と単独選択 | PASS | E2E・画像02 |
| 回転・つけ直し・取消時の元Part保持 | PASS | Unit・E2E |
| Undo／Redo・Save／Reload | PASS | 完全モデル比較 |
| LEVEL 2／Studioの詳細値保持 | PASS | 既存および追加E2E |
| キーボード・タッチ・ドラッグ誤配置防止 | PASS | 入力E2E |
| 全品質ゲート | PASS | 上記ログ |

スクリーンショットは3枚とも目視確認済みです。

- ① 板の周囲にタイヤ候補が4つ表示 (docs/screenshots/placement-01-wheel-candidates.png)
- ② タイヤ配置後、候補を消して黄色い枠で選択 (docs/screenshots/placement-02-wheel-focused.png)
- ③ 元パーツを保持したつけ直し候補 (docs/screenshots/placement-03-reattach.png)

Known Issuesとして、Wheelは既存Raycast Vehicle方式のため独立した側面Colliderはありません。また、任意の面への配置や部品同士の重なりの自動解消は対象外です。タッチはChromiumのエミュレーションで検証しており、実モバイル端末の性能とFirefox／WebKitは未検証です。
