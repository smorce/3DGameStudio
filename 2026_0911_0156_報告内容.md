# 指示5 実装報告

`feat/panel-surface-aerodynamics` 向けに、指示5のPanel Surface Aerodynamicsを実装しました。Wing専用機能を廃止し、Panelの形状・姿勢・位置と接続状態から空力・物理挙動が生まれる構成へ移行しています。

## 実装内容

### SchemaとMigration

- `schemaVersion` を3へ更新。
- Wingを新規Part Kind、Palette、Renderer、Templateから削除。
- v2のWingと旧Panelを新しい正方形Panelへ正規化。
- Panelの幅・長さを `PANEL_SIDE = 1` に固定し、厚さだけを許容範囲内で変更可能にした。
- Panelの質量を密度250と厚さから導出。
- 旧車両のWheel接続位置を新しいPanel基準へ移行し、既存World／Courseの走行を維持。

### Panelと配置

- Panelを固定正方形Visual／Colliderとして生成。
- 厚さ変更時にVisual、Collider、質量を同時更新。
- Panelの4辺にStructural Connectorを追加。
- Panel同士のEdge-to-Edge接続を追加。
- `part.tilt` を追加し、Attachment PointをPivotとしてPanelを傾斜。
- `part.turn` を90度単位の回転へ修正。

### Surface Aerodynamics

- `packages/aerodynamics` にRapier非依存の純粋計算を追加。
- 各PanelのWorld Normal、Panel位置の速度、風速、面積から空気力を計算。
- 揚力係数 `CL = 1.1 * sin(2α)`、抗力係数 `CD = 0.03 + 1.25 * normalAlongFlow²` を使用。
- 動的圧力 `q = 0.5 * ρ * speed²` を使用。
- 各PanelのWorld Positionへ `addForceAtPoint` で空気力を適用。
- Hingeに接続されたPanelも同じ空力計算の対象にした。

### ThrusterとMotor

- Thrusterの推力をThruster自身のWorld Positionへ `addForceAtPoint` で適用。
- MotorのWheel Global Boostを削除。
- 明示的にHingeへ接続されたMotorだけが対応するRevolute Jointを駆動。
- Step開始時に外力とTorqueをリセットし、前フレームの残留を防止。
- Raycast Vehicleの小型Panel向けサスペンションを調整し、旧データ移行後の走行を安定化。

### UI、Renderer、Documentation

- Easy／Creator／StudioでWing表記とWing専用操作を削除。
- Panelの厚さ編集、Tilt、空力情報をUIへ反映。
- 初期車両を2×5枚の正方形Panel、中央Block、4輪のスターター構成に更新し、Panel上の不要なシートVisualを削除。
- Panel、Tilt、Hinge制御面のVisual Evidenceを追加。
- `docs/aerodynamics.md`、`docs/progress.md`、関連ADRと既知の制約を更新。

## 変更した主なファイル

- `packages/aerodynamics/`
- `packages/project-schema/src/index.ts`
- `packages/machine-system/src/index.ts`
- `packages/physics-rapier/src/index.ts`
- `packages/command-system/src/index.ts`
- `packages/renderer-three/`
- `packages/ui-easy/`
- `packages/ui-creator/`
- `packages/ui-studio/`
- `tests/unit/`
- `tests/integration/`
- `tests/e2e/`
- `docs/aerodynamics.md`
- `docs/progress.md`

## 品質ゲート

| チェック         | 結果                             |
| ---------------- | -------------------------------- |
| `pnpm typecheck` | PASS                             |
| `pnpm lint`      | PASS                             |
| `pnpm test`      | PASS（11 files / 79 tests）      |
| `pnpm build`     | PASS（Studio / Player / Server） |
| `pnpm test:e2e`  | PASS（Chromium 18 tests）        |
| `pnpm smoke`     | PASS（12 tests）                 |
| `ReadLints`      | エラーなし                       |

`pnpm format:check` は、今回触っていない既存の `2026_0909_1650_報告内容.md`、`指示4.md`、`指示5.md` の未整形警告によりWorkspace全体では未達です。今回の実装ファイル、テスト、報告書は整形済みです。

## Visual Evidence

- `docs/screenshots/square-panel-aircraft.png`
- `docs/screenshots/tilted-panels.png`
- `docs/screenshots/hinge-control-surface.png`

判定: **COMPLETE**
