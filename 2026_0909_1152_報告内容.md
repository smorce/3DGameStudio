指示2.md の作業を完了しました。完成条件28項目すべて PASS、判定は COMPLETE です。

作業ブランチは `feat/world-runtime-foundations` です。
最終報告書 (docs/world-runtime-report.md)に、Architecture、全74変更ファイル、Schema Migration、テスト結果、性能比較、完成条件ごとの Requirement／Status／Evidence 表 を収録しました。

実装した6項目は次のとおりです。

- **物理チャンク**：地形、World Entity、選択コースの道路・障害物をMachine周辺だけ生成。先行ロードと保持半径で境界通過を保護します。
- **Instancing**：同一素材をチャンク単位でまとめ、glTF Templateを共有。instanceIdから元のEntityを選択できます。
- **LOD**：LOD0／1／2を生成し、バッチ単位で距離切替。ヒステリシスと読込失敗時のフォールバックを備えます。
- **Collider改善**：Box／Convex Hull／Trimeshに対応。Geometryは別ファイルで共有し、破損・生成失敗時はBoxへ戻します。
- **非破壊最適化・容量設計**：Originalを保持し、RuntimeだけMeshopt／WebPで最適化。Binary Upload、設定可能な安全上限、StorageとRuntimeの予算分離を実装しました。
- **コース選択**：LEVEL 2／Studioから走行対象を選択・保存。CommandによるUndo／Redoに対応し、選択外コースのColliderを除外します。

保存形式は Schema v2 に更新し、既存3デモの v0／v1→v2→保存→再読込 を確認しています。

| チェック | 結果 |
| --- | --- |
| typecheck | PASS |
| lint・循環依存 | PASS |
| format | PASS |
| 単体 | 28件 PASS |
| 統合 | 19件 PASS |
| 全テスト | 47件 PASS |
| E2E | 11件 PASS |
| smoke | 9件 PASS |
| build | Studio／Player／Server PASS |

既存テストは維持しています。
品質ゲートと実行ログ (docs/evidence/world-runtime-quality-gates.json)を保存しました。

性能比較は、元コミットと変更後の本番ビルドを同じ条件で測定しました。

| 指標 | Before | After |
| --- | ---: | ---: |
| Draw Calls | 219 | 32 |
| 描画Triangles | 16,708 | 30,668 |
| 描画Chunks | 25 | 25 |
| Physics Chunks | 一括生成 | 25 |
| Colliders | 1,503 | 526 |
| 同一外部素材のRuntime LOD0 | 286,324 bytes | 38,896 bytes |

Draw Callsは約85%減り、遠方の岩1,000個のColliderが解放されました。
一方、この画角ではバッチ単位の描画範囲判定によりTrianglesが増えています。
外部素材のLODでは 3,968→1,785→475 triangles に減少しています。
測定条件・生データ・制約 (docs/performance.md)も記録しました。

Origin Rebasing、Raycast Vehicle固有の制約、簡易浮力・揚力、Servo等、Loop／Tunnel専用形状、ambientCG ZIP、Undo履歴永続化、Cloud、実AI／Blender接続は、指定どおり今回の対象外としてKNOWN_ISSUES.mdに残しています。
