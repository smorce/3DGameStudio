# Motor物理モデル分離 実装報告

## 1. 調査結果

`feat/panel-surface-aerodynamics`をremoteの最新HEADへ同期しました。確認したHEADは`b972bc3f9f345531ab6228002e4886dd43a1ef47`です。

従来のMotorは、`motorTorque`を最大出力と`throttle * motorTorque / 20`の目標角速度へ同時に使用していました。WheelはRaycast Vehicle、ThrusterはWorld Positionへの`addForceAtPoint()`、PanelはSurface Aerodynamicsという独立した経路で、MotorのGlobal Wheel Boostは既に存在しませんでした。

## 2. Rapier Motor APIの確認結果

実インストール版は`@dimforge/rapier3d-compat 0.19.3`です。公開型定義には`RevoluteImpulseJoint.configureMotorVelocity()`、`configureMotorModel()`、`configureMotor()`がありますが、最大Impulse・最大Torqueを指定する公開パラメータやメソッドはありませんでした。`configureMotorVelocity()`の第2引数`factor`を最大Torqueと推測して使うことは避けました。

## 3. 採用したMotor物理方式

方式Bを採用しました。RapierのRevolute Jointは回転軸以外の拘束に使用し、Motor出力は公開RigidBody APIの`addTorque()`で適用します。

固定Stepごとに次を計算します。

    targetVelocity = clamp(throttle, -1, +1) * targetAngularVelocity
    relativeAngularVelocity =
      dot(bodyB.angvel - bodyA.angvel, axisWorld)
    desiredTorque =
      controllerGain * (targetVelocity - relativeAngularVelocity)
    appliedTorque =
      clamp(desiredTorque, -maxTorque, +maxTorque)

`controllerGain`は40 N·m/(rad/s)です。したがって速度誤差が1 rad/sなら40 N·mを要求し、`maxTorque`が180 N·mなら要求値400 N·mは180 N·mへ制限されます。

`axisWorld`はBody Aの現在姿勢と保存されたJoint軸から毎Step再計算します。Body Bへ`+axisWorld * appliedTorque`、Body Aへ`-axisWorld * appliedTorque`を加えるため、作用と反作用が両Bodyへ伝わります。RigidBodyのrotationやangvelは直接変更していません。

`throttle === 0`または`actuator.enabled === false`ではMotorの能動Torqueを加えません。目標0 rad/sへ戻すMotor Brakeにはせず、Hingeの既存dampingだけを受動Torqueとして処理します。Torqueは連続Torque（N·m）なので、Impulseへ変換するための`dt`除算は行っていません。固定Stepは1/60秒です。

## 4. Schema変更

- `schemaVersion`を3から4へ更新しました。
- `actuator.targetAngularVelocity`を追加しました。
- `actuator.motorTorque`はMotorの最大Torque（N·m）として扱います。
- 新規Motorの既定値は`motorTorque = 180`、`targetAngularVelocity = 9`です。
- Wheel、Thruster、その他Partの既存`motorTorque`の意味は維持し、未使用の`targetAngularVelocity`は0で初期化します。

## 5. Migration

v3 Projectを読み込むと、Motorについて旧仕様の互換値である`oldMotorTorque / 20`を`targetAngularVelocity`へ設定します。`motorTorque`自体は変更せず、v3のMotor速度を可能な限り維持します。Motor以外のPartには0を補完します。

v0、v1、v2からの既存Migration後にもv3→v4を適用するため、旧Projectを読み込めます。

## 6. Physics変更

- Hingeの実Rapier Revolute Jointを維持しました。
- HingeごとにBody A、Body B、Joint軸、dampingをRuntimeへ保持します。
- Passive Hinge DampingとMotor Torqueを別々に適用します。
- Motorは対応Hingeへ明示的に固定接続された場合だけRuntime driverになります。
- 未接続MotorはTorqueを発生させず、Machine全体やWheelへ追加出力を与えません。
- Wheelの`setWheelEngineForce()`、Thrusterの`addForceAtPoint()`、Panelの空力Force-at-pointは変更していません。

## 7. UI変更

- LEVEL 1のPaletteと配置では物理単位や数値を表示しません。
- LEVEL 2のMotor Inspectorでは「回転の速さ」「力強さ」の2スライダーを表示します。
- StudioのMotor Inspectorでは「最大トルク（N·m）」「目標角速度（rad/s）」を数値編集できます。
- 保存値は常にN·mとrad/sです。
- Commandは既存の`part.update`を利用し、新しいCommand種別は追加していません。

## 8. Test追加内容

- v3 Motorのv4 Migrationと旧速度式の互換値。
- MotorなしHingeの受動回転。
- 未接続Motorの無作用。
- 正負throttleの回転方向。
- throttle 0と`enabled = false`の能動Torque停止。
- 同じ目標速度でmaxTorqueを変えた加速差。
- 重い固定構造による加速低下。
- 同じmaxTorqueでtargetAngularVelocityを変えた目標速度差。
- Body A／Body Bの作用反作用。
- LEVEL 2の物理単位非表示とStudioの2値保存・再読込。
- 既存Car、Thruster、Panel Surface Aerodynamicsの回帰。

## 9. 実行した品質ゲート

- `pnpm typecheck`: PASS
- `pnpm lint`: PASS（循環依存なし、既存のMadge warning 4件）
- `pnpm test`: PASS（11 files / 90 tests）
- `pnpm build`: PASS（Studio / Player / Server）
- `pnpm test:e2e`: PASS（Chromium 19 tests）
- `pnpm smoke`: PASS（12 tests）
- `pnpm format:check`: FAIL（今回触っていない既存の`2026_0909_1650_報告内容.md`、`指示4.md`、`指示5.md`）
- `ReadLints`: エラーなし

Rapier初期化時のdeprecated parameters警告、Build時のZod annotation・大きなChunk警告は既存の依存・構成由来で、今回のTest失敗にはなっていません。

## 10. 変更ファイル一覧

- `README.md`
- `KNOWN_ISSUES.md`
- `docs/project-schema.md`
- `docs/machine-system.md`
- `docs/testing.md`
- `docs/progress.md`
- `packages/project-schema/src/index.ts`
- `packages/machine-system/src/index.ts`
- `packages/physics-rapier/src/index.ts`
- `packages/ui-creator/src/index.tsx`
- `tests/unit/model.test.ts`
- `tests/unit/world-runtime.test.ts`
- `tests/integration/physics.test.ts`
- `tests/e2e/placement.spec.ts`
- `2026_0911_0927_報告内容.md`

## 11. 残っている制約

Rapier 0.19.3の公開Joint APIだけではJoint Motorの最大Impulseを指定できないため、今回の方式Bでは比例制御ゲインとBody Torque適用をRuntime側で管理しています。将来Rapierに正式な最大Impulse APIが追加された場合は、APIの単位を再確認した上で方式Aへ移行できます。

MotorとHingeの接続判定は既存の明示的な固定接続関係を使います。Motorを置いただけで任意のHingeを自動選択する機能や、WheelをMotorと同じJointで直接駆動する機能は追加していません。

## 12. 今後改善するなら何をするか

Rapierの公開最大Torque／最大Impulse APIが利用可能になった場合に、方式Aとの挙動比較を追加します。また、Motor・Hinge・Panelの接続候補を同時に扱える専用Connector設計、controllerGainのStudio詳細編集、実測した慣性値に基づくMotorプリセットを将来検討します。
